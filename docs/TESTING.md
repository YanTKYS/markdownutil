# テスト記録

## v0.1.0

実施日: 2026-08-13
方法: `python3 -m http.server` で `markdownutil/` を静的配信し、実ブラウザ（Chromium /
Playwright経由）から操作して確認。文書サンプルはWord/Excel/PowerPoint/PDFはPython
（python-docx / openpyxl / python-pptx / reportlab）で、ODTは仕様に沿って直接組み立てて生成。

**このテストの位置づけ**: ここでのサンプルはいずれもプログラムで生成した単純な文書であり、
見出し・段落・箇条書き・単純な表といった基本的な構造要素が変換できることの確認が目的。
自治体等の実業務で使われる文書にありがちな、結合セルを多用した帳票Word、複雑な図形を
含むPowerPoint、レイアウトの複雑なPDF等は対象に含まれていない。したがって本記録が示すのは
「基本的な変換性能を確認できた」という段階までであり、実業務文書での変換品質はIIS実機配置後、
庁内の実文書を用いて別途確認する。

### 1. 変換テスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | DOCX → Markdown（見出し・段落・太字/斜体・箇条書き・番号付きリスト・表を含む文書） | OK。すべて意図通りにMarkdown化された |
| 2 | XLSX → Markdown（2シート、セル値・表構造） | OK。シートごとに`##`見出し＋表として出力された |
| 3 | PPTX → Markdown（タイトルスライド・本文スライド・表スライド） | OK。スライドごとに見出し・箇条書き・表として出力された |
| 4 | テキストを含むPDF → Markdown（2ページ、複数段落） | OK。見出し・段落・ページをまたいだ内容が抽出された |
| 5 | CSV → Markdown | OK。表として変換された |
| 6 | その他代表形式（RTF, ODT） | OK。RTFは書式付きの段落として、ODTは見出し・箇条書き・表として変換された |

### 2. 変換品質の確認

#### Word

- 見出し（`#`〜）: 正しいレベルで出力
- 段落: 正しく分割
- 箇条書き・番号付きリスト: `-` / `1.` 形式で正しく出力
- 表: ヘッダー行・セル内容とも正しく`|`区切りの表に変換

#### Excel

- セル値: 正しく反映
- 表構造: 見出し行を含め正しく罫線付きMarkdown表へ変換
- 複数シート: シートごとに`##`見出しで区切られ、シート名も保持される

#### PowerPoint

- スライド単位の内容: スライドごとに区切られて出力
- タイトル: 見出しとして出力
- 本文: 箇条書きとして出力
- 表: スライド内の表も正しく変換

#### PDF

- 段落: 判定され改行される
- 改行: テキストの配置に応じて段落として結合される（reportlabで意図的に分けた2文が
  1段落にまとまるなど、元のレイアウト情報に依存する挙動を確認。README「既知の制限」に記載）
- 表: 本テストのサンプルには表を含めていないため未確認
- 複数ページ: 2ページ目の内容も正しく抽出された

上記より、AnyDoc WASMは見出し・段落・箇条書き・単純な表といった基本的な構造要素を
安定してMarkdownへ変換できることを確認した。ただし対象はプログラムで生成した単純な
サンプル文書であり、結合セルを多用した帳票やレイアウトの複雑な文書は含まれていない。
「基本的な変換性能を確認した」段階にとどまり、自治体等の実業務文書での精度は実機検証が
必要。PDFは元データの構造に変換品質が依存するため、特に追加検証が必要。

### 3. Markdownの基本機能テスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 7 | `.md` を直接開く | OK。AnyDocを経由せずそのまま読み込まれた |
| 8 | `.txt` を直接開く | OK。そのまま読み込まれた |
| 9 | Markdownを編集できる | OK。`textarea`で通常のテキスト編集が可能 |
| 10 | 編集内容がプレビューへ反映される | OK。入力のたびに軽いdebounce（150ms）を挟んで反映 |
| 11 | 表がプレビューできる | OK |
| 12 | コードブロックが表示できる | OK（フェンス`` ``` ``によるコードブロック、インラインコードとも表示確認） |

見出し・段落・太字/斜体・箇条書き・番号付きリスト・引用・水平線・リンクについても
markdown-itの標準レンダリングで表示されることを確認した。

### 4. 操作テスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 13 | ファイル選択（「ファイルを開く」ボタン） | OK |
| 14 | ドラッグ&ドロップ（合成`DragEvent`による検証） | OK。ドロップ領域にドラッグ中はオーバーレイ表示、ドロップで変換開始 |
| 15 | Markdownコピー（Clipboard API） | OK。「Markdownをコピーしました」を表示し、クリップボード内容が編集内容と一致することを確認 |
| 16 | `.md`保存 | OK。`sample.docx`変換後の保存ファイル名が`sample.md`になることを確認。`.md`を直接開いた場合は元のファイル名のまま保存されることも確認 |
| 17 | クリア | OK。内容がある場合のみ確認ダイアログを表示し、承諾後にMarkdown・プレビュー・ファイル名・ステータスが初期状態に戻ることを確認 |

### 5. 異常系テスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 18 | 非対応形式（`.html`） | 「この形式は変換に対応していません。」と表示され、画面は壊れない |
| 19 | 壊れたファイル（DOCXを途中で切断） | 「ファイルが破損しているか、読み取れない構造のため変換できませんでした。」と表示 |
| 20 | テキスト抽出できないPDF（画像のみのPDF） | 「このPDFからテキストを取得できませんでした。スキャンPDFには対応していない可能性があります。」と表示（AnyDocのエラーメッセージに"Scanned"の語が含まれることを確認した上で専用メッセージに変換） |
| 20' | 暗号化PDF | 「このファイルはパスワード保護（暗号化）されているため変換できません。」と表示 |
| 21 | WASM読み込み失敗時の表示 | `converter.js`の`ensureInit()`が失敗時に「WASMモジュールの初期化に失敗しました。ページを再読み込みしてください。」を返すことをコードレベルで確認（正常環境のため実際の初期化失敗は意図的な障害注入なしには再現していない） |

いずれの異常系でも画面全体がクラッシュせず、直前の編集内容が保持されたままエラーメッセージが
表示されることを確認した。

### 6. 安全性（プレビューのサニタイズ）テスト

Markdown内に以下を混入させ、実行されないことを確認した。

- `<script>window.__xssFired = true;</script>` → エスケープされテキストとして表示。実行されない
- `<img src=x onerror="window.__xssFired = true">` → エスケープされテキストとして表示。実行されない
- `[bad](javascript:window.__xssFired = true)` → markdown-itの既定のリンク検証により
  リンクとして解釈されず、実行されない
- 通常の`https://`リンクは正しく`<a href="...">`として描画されることも合わせて確認

### 7. 閉域確認

| # | 内容 | 結果 |
| --- | --- | --- |
| 22 | CDNを利用していない | OK。`vendor/`配下のローカルファイルのみを参照 |
| 23 | 外部APIを利用していない | OK |
| 24 | 外部通信が発生しない | OK。Playwrightでリクエストを監視し、配信元オリジン（`http://127.0.0.1:8123`）以外へのリクエストが0件であることを確認（文書変換・プレビュー描画・コピー・保存の一連操作を含む） |
| 25 | 静的Webサーバだけで動作する | OK。`python3 -m http.server`（Pythonのみの単純な静的サーバ）だけで全機能が動作した。`.wasm`は`application/wasm`で配信されることも確認 |

### v0.1.0まとめ

DOCX / XLSX / PPTX / PDF（テキスト含む） / CSV / RTF / ODT の変換、Markdown編集・
プレビュー、ファイル選択・ドラッグ&ドロップ・コピー・保存・クリアの各操作、非対応形式・
破損ファイル・スキャンPDF・暗号化PDFの異常系、プレビューの安全性、閉域環境での動作
（外部通信ゼロ）を、実ブラウザ上で一通り確認した。AnyDoc WASMは基本的な文書構造の変換に
ついて実用的な性能を確認できた一方、テストに用いたのはプログラム生成の単純なサンプル文書
であり、結合セルの多い帳票や複雑なレイアウトの実業務文書は未検証。PDFの改行・段落判定は
元データ構造に依存するという制限もある。

## v0.2.0（Marpスライドプレビュー統合）

実施日: 2026-08-13
方法: v0.1.0と同じく`python3 -m http.server`で静的配信し、Playwright経由のChromiumで操作を
自動化して確認。参考実装の[`slide`（iSlide）リポジトリ](https://github.com/YanTKYS/slide)を
`git clone`し、`iSlide.html`・`build/entry.js`・README等を読んだ上で、Marp Coreの初期化
オプション・iframe分離方式・テーマfront matter読み書き・HTML出力・印刷・プレゼン表示の
実装方式を確認してから、MarkdownUtil向けに`js/slide-preview.js`として再実装した。

### 通常プレビュー回帰

v0.2.0の変更がv0.1.0の既存機能を壊していないことを確認した。

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | markdown-itによる通常プレビューが従来どおり動作 | OK。DOCX変換後、文書モードのプレビューに見出し・表が表示された |
| 2 | DOCX等→AnyDoc→Markdown変換が従来どおり動作 | OK。`sample.docx → Markdown変換完了`のステータス、変換結果も従来と同一 |
| 3 | Markdownコピー | OK |
| 4 | Markdown保存 | OK（`sample.md`としてダウンロード） |
| 5 | ドラッグ&ドロップ | OK（合成`DragEvent`によるCSVドロップで変換完了を確認） |

### スライド

| # | 内容 | 結果 |
| --- | --- | --- |
| 6 | Markdown→Marpスライド表示 | OK |
| 7 | `---`による複数スライド | OK。2枚のMarkdownで「2枚」とスライド枚数表示、iframe内のSVGも2個 |
| 8 | front matterの読み取り | OK。`theme: default`のMarkdownでテーマ選択欄が`default`に同期 |
| 9 | defaultテーマ | OK |
| 10 | gaiaテーマ | OK。テーマ選択で切替時、front matterへ`theme: gaia`が書き込まれることを確認。**リモートWebフォントの`@import`が除去されていることも確認（後述の閉域確認を参照）** |
| 11 | uncoverテーマ | OK |
| 12 | 表 | OK。iframe内に`<table>`が生成されることを確認 |
| 13 | 箇条書き | OK。iframe内に`<ul>`が生成されることを確認 |
| 14 | コードブロック | OK。iframe内に`<pre>`/`<code>`が生成されることを確認 |

AnyDocが生成した（front matterを含まない）Markdownをスライドモードで表示した場合も、
エラーにならず「1枚」として表示されることを確認した。また、スライドモードを表示しただけでは
front matterが自動挿入されないこと（Markdown本文がモード切替の前後で完全に一致すること）も
確認した。

### 操作

| # | 内容 | 結果 |
| --- | --- | --- |
| 15 | 文書／スライド切替 | OK。切替時にMarkdown本文は変化せず、双方のプレビューが即座に更新される。v0.2.1で、文書モード時に`#slide-toolbar`等の`computed display`が実際に`none`になっていること（属性だけでなく描画結果）も確認するよう強化（詳細は「v0.2.1」参照） |
| 16 | HTML出力 | OK。`sample.html`としてダウンロードされ、内容に`data-marpit-svg`のSVGとCSSが内包されていること、リモート`@import`が含まれないことを確認 |
| 17 | 印刷 | OK。「印刷」ボタンでBlob URLの新規タブ（印刷用スタンドアロンHTML、`window.print()`を自動実行）が開くことを確認 |
| 18 | プレゼン表示 | OK。ツールバー・編集領域が非表示になり、スライドが全画面表示、右下に「1 / 2」等のヒントが表示される |
| 19 | キーボードによるスライド移動 | OK。`→`で次、`←`で前、`Home`で先頭、`End`で最後へ移動し、ヒントの表示が追従することを確認 |
| 20 | Escによる復帰 | OK。`Esc`でプレゼン表示を終了し、ツールバー・編集領域の表示が復帰することを確認 |

### 閉域

| # | 内容 | 結果 |
| --- | --- | --- |
| 21 | Marp Coreがローカルファイルから読み込まれる | OK。`vendor/marp/marp-core.bundle.mjs`（自前ビルドのESM）をimportして使用 |
| 22 | CDNアクセスなし | OK |
| 23 | 外部APIアクセスなし | OK |
| 24 | Marpテーマ由来のWebフォント通信なし | OK。`gaia`テーマのCSSには本来`@import "https://fonts.bunny.net/..."`が含まれるが、`slide-preview.js`の`removeRemoteImports()`で除去されていることをiframe内のスタイル要素から直接確認した |
| 25 | twemoji等への通信なし | OK。`emoji: { shortcode: true, unicode: false }`により絵文字を画像化しない設定を確認（コードレベルおよび描画結果に`<img>`が生成されないことで確認） |

Playwrightで全操作を通してリクエストを監視し、配信元オリジン以外への通信が0件であることを
確認した（文書変換・通常プレビュー・スライドレンダリング・テーマ切替・HTML出力・印刷・
プレゼン表示のすべての操作を含む）。ブラウザの`console`エラーも0件だった。

### v0.2.0まとめ

v0.1.0の機能（AnyDoc変換・通常プレビュー・コピー・保存・ドラッグ&ドロップ）が壊れていないこと、
および新規のスライドプレビュー機能（Marpレンダリング・3テーマ・表/リスト/コードブロック・
モード切替・HTML出力・印刷・プレゼン表示とキーボード操作）が一通り動作することを実ブラウザで
確認した。特にgaiaテーマのリモートWebフォント`@import`が確実に除去されていることを、
iframe内に実際に反映されたCSSを読み取って確認した点は、閉域要件との整合性を確かめる上で
重要な検証である。

### 既知の制限（v0.2.0で追加）

- スライドプレビューは、AnyDocが生成した通常のMarkdownを自動でスライドへ整形するものではない。
  見出しごとの自動`---`挿入や枚数の自動決定は行わないため、意図した区切りでスライド化するには
  利用者がMarkdown側に`---`やfront matterを追記する必要がある。
- 元のMarkdown中に水平線としての`---`単体行が含まれる場合、Marpの仕様上スライド区切りとして
  解釈される。AnyDocが変換した文書に水平線が含まれるケースは今回のテストでは確認していない。
- スライド用のテスト対象はMarp記法として典型的な内容（見出し・箇条書き・表・コードブロック・
  テーマ切替）にとどまり、数式（MathJax）・画像埋め込み・スピーカーノート等、Marpのより
  高度な機能は今回個別に検証していない。

## v0.2.1（スライドモード表示制御の回帰修正）

実施日: 2026-08-13
方法: v0.2.0と同じくPlaywright経由のChromiumで確認。新機能は追加せず、CSSの1箇所を修正した。

### 修正した問題

`css/app.css`の`.slide-toolbar`が`display: flex`を無条件に指定していたため、
`index.html`側の`hidden`属性（HTMLの`hidden`属性はUAスタイルシートの
`[hidden] { display: none }`によって非表示になる）が、詳細度に関係なく作者スタイルシートの
`display`指定によって上書きされ、文書モードでもスライド専用ツールバー（テーマ・HTML出力・
印刷・プレゼン表示）が実際には表示されてしまう状態だった。v0.2.0時点のテストは`hidden`
「属性」の有無（`getAttribute('hidden')`）のみを確認しており、実際の描画結果
（`getComputedStyle().display`）までは確認していなかったため、この問題を検出できていなかった。

`.slide-toolbar`から`display: flex`を外し、`.slide-toolbar:not([hidden]) { display: flex; }`
へ変更することで、`hidden`属性がある限り確実に非表示になるようにした。同様のパターンが
他の要素（`#preview` / `#slide-frame` / `#presenter-hint`）にも無いか確認したが、これらは
`display`を明示的に指定していないため、`hidden`属性は元から正しく機能していた。

### 追加確認したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 文書モード時、`#slide-toolbar`の`hidden`属性が付与されている | OK |
| 2 | 文書モード時、`#slide-toolbar`の`getComputedStyle().display`が`none`である（修正前は`flex`のまま表示されていた） | OK（修正後） |
| 3 | 文書モード時、テーマ選択欄・HTML出力・印刷・プレゼン表示ボタンが`isVisible()`で不可視 | OK |
| 4 | スライドモードへ切替後、`#slide-toolbar`の`getComputedStyle().display`が`flex`になり、各操作ボタンが`isVisible()`で可視になる | OK |
| 5 | スライドモードから文書モードへ戻すと、再び`display: none`・不可視に戻る | OK |
| 6 | `#preview` / `#slide-frame`のモード切替時の`getComputedStyle().display`（`none`⇔表示）も合わせて確認 | OK（元から問題なし） |
| 7 | v0.2.0で実施した全テスト（AnyDoc変換・通常プレビュー・コピー・保存・D&D・スライド表示・テーマ・HTML出力・印刷・プレゼン表示・キーボード操作・クリア） | OK（回帰なし） |
| 8 | 外部通信・consoleエラー | 0件（回帰なし） |

### v0.2.1まとめ

スライド専用ツールバーが文書モードでも実際には表示されてしまう回帰を修正した。原因は
CSSの`display`指定がHTMLの`hidden`属性を上書きしていたことで、`:not([hidden])`で
スコープすることで解決した。今後同様の問題を早期に検出できるよう、モード切替の確認は
DOM属性だけでなく`getComputedStyle().display`と`isVisible()`の両方で行うようにした。

## v0.2.2（バグ修正とリファクタリング）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。遅い端末での挙動はCDPのCPUスロットリング
（4倍・8倍）で再現した。

### 修正1: 起動時にMarp Coreを読み込まないようにした

**問題**: `slide-preview.js`が`vendor/marp/marp-core.bundle.mjs`（約3.8MB）を静的importして
いたため、その解析・実行が終わるまで`app.js`が動き出せず、画面は表示されているのにボタンが
一切反応しない時間が生じていた。文書変換しか使わない職員も必ずこの待ち時間を負担していた。

**修正**: スライドモードが初めて使われたときに動的import（`import()`）で読み込むようにした。

| CPU速度 | 修正前（操作可能まで） | 修正後 |
| --- | --- | --- |
| 4倍遅い | 1374ms | 246ms |
| 8倍遅い | 2333ms | 453ms |

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 起動時にMarp Coreを読み込まない | OK（リソース取得なし） |
| 2 | 文書モードのみの利用ではMarp Coreを読み込まない | OK |
| 3 | 初めてスライドモードへ切り替えたときに読み込まれ、正しく描画される | OK（2枚のMarkdownでSVG 2個） |
| 4 | 読み込み中は「スライドを準備しています...」と表示される（2回目以降は描画前に上書きされ表示されない） | OK |

**副次的な効果（IIS配置時の耐性向上）**: `vendor/marp/marp-core.bundle.mjs`が配信できない
場合（IISで`.mjs`が未登録＝404、MIME不正など）の挙動を、修正前後で比較した。

| | 文書の変換・編集・通常プレビュー | スライドモード |
| --- | --- | --- |
| 修正前 | **画面全体が動作しない**（静的importの失敗で`app.js`自体が実行されない） | 動作しない |
| 修正後 | 正常に動作する | 「スライド表示機能を読み込めませんでした。ページを再読み込みしてください。」と表示 |

### 修正2: プレゼン表示中にブラウザ操作を奪わないようにした

**問題**: プレゼン表示中は押されたキーをすべて`preventDefault()`していたため、`F5`（再読み込み）や
`Ctrl+P`（印刷）などのブラウザ標準操作がまったく効かなくなっていた。

**修正**: 修飾キー（Ctrl/Alt/Meta）付きの操作は対象外とし、スライド操作に使うキーだけ
既定動作を抑止するようにした（`handlePresentationKey()`が処理したキーのみtrueを返す）。

| # | 内容 | 結果 |
| --- | --- | --- |
| 5 | プレゼン中の`F5` / `Ctrl+P` / 一般文字キーが抑止されない | OK（いずれも`defaultPrevented=false`） |
| 6 | プレゼン中の`←` `→` `Home` `End` `Esc`は従来どおり動作する | OK |

### 修正3: スライドのエラー表示が解消後も残る問題を修正した

**問題**: スライドの解析エラーを共有のステータスバーへ表示していたため、Markdownを直しても
エラー文言が残り続けていた（成功時にクリアする処理が無かった）。

**修正**: スライドの状態（枚数・エラー）はスライドツールバー内の専用表示へ移した。描画のたびに
上書きされるため、エラーが解消すれば自動的に消える。ファイル変換等のステータスバーとも混ざらない。

| # | 内容 | 結果 |
| --- | --- | --- |
| 7 | 解析エラー時に専用表示へ赤字でメッセージが出る | OK |
| 8 | エラー時もステータスバー（変換結果等）を上書きしない | OK |
| 9 | Markdownを直すと次の描画でエラー表示が自動的に消える | OK |
| 10 | スライド未描画の状態でHTML出力・印刷・プレゼン表示を押しても、それぞれ適切な案内が出る | OK |

なお、Marp Coreは不正なfront matter（タブインデント・閉じていないフロー記法・重複キー等）を
かなり寛容に受け付けるため、実際に解析エラーを発生させることは難しい。上記7〜9は、Marpの
`render()`が失敗する状況を意図的に作って確認した。

### リファクタリング

動作を変えずに、次の点を整理した。

- iframeへの送信前の待ち行列（`pendingMessage`、1件しか保持できなかった）を廃止し、
  iframeの準備完了時に現在の状態をまとめて送り直す`syncFrame()`に置き換えた。処理が短くなり、
  iframeが読み込み直された場合にも表示を復元できる。
- プレゼン表示のキー処理を`if`の連鎖から`switch`へ変更し、「処理したかどうか」を返すようにした。
- `writeTheme()`の文字位置計算に説明コメントを追加し、`theme:`行の組み立てを
  `withThemeLine()`として切り出した。
- `openPrintWindow()`の戻り値を`{ opened: boolean }`から`boolean`へ簡素化した。
- 使われていない`isPresenting()`を削除した。
- スライド操作の事前チェックを`hasSlides()`の1箇所へまとめた。

`js/converter.js`と`js/preview.js`（AnyDoc・markdown-it側）は、IIS実機検証でv0.1.0との
差分を切り分けやすくするため、今回も変更していない。

### 回帰確認

v0.1.0・v0.2.0・v0.2.1で実施したテストを再実行し、いずれも回帰がないことを確認した
（DOCX変換・通常プレビュー・コピー・保存・ドラッグ&ドロップ・クリア・スライド表示・
3テーマ・gaiaのリモート`@import`除去・HTML出力・印刷・プレゼン表示・キーボード操作・
文書モードでのスライドツールバー非表示）。外部通信0件、consoleエラー0件。

## v0.2.3（スライド出力の状態整合性修正）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。Marpの`render()`を差し替え、成功/失敗を明示的に
切り替えられるようにした上で検証した。

### 修正した問題

Marpの解析に失敗した場合、`slide-preview.js`は直前の正常なプレビュー（`lastRendered`/
`slideCount`）を画面に残す設計だった。これ自体は意図どおりだが、`app.js`側の`hasSlides()`は
「`slideCount > 0`か」だけを見て出力系操作（HTML出力・印刷・プレゼン表示）を許可していたため、
以下の手順で**「今のMarkdownではなく、直す前の内容」が出力できてしまっていた**。

```text
正常なMarkdown（1枚表示） → HTML出力は正常内容
        ↓
Markdownを壊す → 画面には解析エラー表示、スライド自体は直前の内容のまま
        ↓
「HTML出力」を押す → 壊す前の内容が保存されてしまう（バグ）
```

実際に強制的に解析を失敗させて再現したところ、HTML出力・印刷・プレゼン表示のいずれも
「壊す前の正常な内容」で実行できてしまうことを確認した。

### 修正

`slide-preview.js`に、直前の表示が現在のMarkdownを正しくレンダリングした結果かどうかを
示す`renderValid`を追加した。`render()`が成功した場合のみ`true`、解析に失敗した場合は
`false`にする（表示自体は直前の内容を残す従来の挙動は変えていない）。`isRenderValid()`を
新たにexportし、`app.js`の`hasSlides()`で`slideCount > 0`に加えて確認するようにした。
解析エラー中に出力系ボタンを押した場合は「Markdownにエラーがあるため実行できません。
スライドの表示内容を確認してください。」と表示し、実行を止める。

### 追加確認したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 正常時、HTML出力・印刷・プレゼン表示がいずれも実行できる | OK |
| 2 | Markdownを壊すと解析エラー表示になり、画面には直前の内容が残る | OK |
| 3 | 解析エラー中にHTML出力を押しても、ダウンロードが発生しない | OK（修正前は壊す前の内容がダウンロードされていた） |
| 4 | 解析エラー中に印刷を押しても、印刷ウィンドウが開かない | OK（修正前は壊す前の内容で開いていた） |
| 5 | 解析エラー中にプレゼン表示を押しても、プレゼン表示に入らない | OK（修正前は壊す前の内容で入れていた） |
| 6 | いずれも「Markdownにエラーがあるため実行できません。」が表示される | OK |
| 7 | Markdownを直すと、直後のHTML出力に現在の内容が反映される（古い内容を含まない） | OK |
| 8 | AnyDoc変換直後（front matterなし）のスライドでHTML出力等が引き続き利用できる（正常系への影響がないこと） | OK |

### v0.2.3まとめ

出力系操作（HTML出力・印刷・プレゼン表示）が、解析エラー中に「直す前の古い内容」を
出力してしまう問題を修正した。表示自体を空にする必要はなく、出力の可否だけを現在の
Markdownの解析結果と一致させることで、利用者が「今編集している内容」と異なるものを
誤って配布・印刷・投影してしまう事態を防いだ。v0.2.2までの全テストを再実行し回帰なし、
外部通信0件・consoleエラー0件を確認した。

## v0.2.4（スライド描画内容とMarkdown編集状態の整合性修正）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。Marpの`render()`を差し替え、入力内容をそのまま
スライドの見出しへ反映するようにして、どの版の内容が出力されたかを判別できるようにした。

### 修正した問題

v0.2.3で追加した`renderValid`は「直前の`render()`が成功したか」だけを表しており、
「その成功結果が今のエディタの内容と一致しているか」までは保証していなかった。そのため
以下の状況で、出力系操作（HTML出力・印刷・プレゼン表示）が**編集中の最新内容ではなく、
直前に描画が完了していた古い内容**を出力できてしまっていた。

- Markdownを編集した直後、プレビュー更新のdebounce（150ms）が完了する前に操作した場合
- モードを切り替えた直後、Marpの再描画（非同期）が完了する前に操作した場合

実際に、debounce待機中（入力から50ms後）にHTML出力を押すと、編集前の内容
（「スライドA」）がダウンロードされてしまうことを確認した。

### 修正

`slide-preview.js`の`renderValid`（真偽値）を`renderedSource`（最後に正しく
レンダリングできたMarkdownそのもの）に置き換えた。`render()`成功時に
`renderedSource = markdown`として入力を記録し、解析失敗時や読み込み失敗時は`null`に
戻す。新たに`isRenderCurrent(markdown)`をexportし、「渡されたMarkdownが、最後に
成功した描画の入力と一致するか」を判定できるようにした。`app.js`の`hasSlides()`は
`isRenderCurrent(editor.value)`で確認するように変更した。

真偽値だけを追跡する方式（編集イベントのたびに`renderValid = false`にする等）ではなく、
入力そのものを記録して比較する方式にしたことで、ファイル読込・テーマ変更・クリアなど
Markdownを書き換える経路が今後増えても、比較対象を追加登録し忘れて判定漏れが起きる
心配がない。

### 追加確認したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 正常時、編集内容をそのままHTML出力・印刷・プレゼン表示できる | OK |
| 2 | 入力直後、debounce完了前（150ms未満）にHTML出力を押すと、ダウンロードが発生しない | OK（修正前は編集前の内容がダウンロードされていた） |
| 3 | debounce完了後に再度押すと、最新内容が出力され、編集前の内容は含まれない | OK |
| 4 | モード切替直後、再描画完了前に操作しても、最新内容が確定してから出力される | OK |
| 5 | debounce待機中は「現在のMarkdownがまだスライドへ反映されていません。」と表示される | OK |
| 6 | v0.2.3の解析エラー時のブロック（front matterが壊れている場合）が引き続き機能する | OK |
| 7 | AnyDoc変換直後（front matterなし）のスライドでHTML出力等が引き続き利用できる | OK |

### v0.2.4まとめ

出力系操作が「直前に描画が完了していた版」ではなく「今エディタに入力されている内容」と
一致する場合のみ許可されるよう修正した。debounce待機中・モード切替直後という、
v0.2.3では想定していなかった「再描画がまだ終わっていない」区間を含めて塞いだ。
判定方法を真偽値から「最後に成功した入力そのものとの比較」に変えたことで、今後
Markdownを書き換える経路が増えても同種の見落としが起きにくい実装にした。v0.1.0〜
v0.2.3までの全テストを再実行し回帰なし、外部通信0件・consoleエラー0件を確認した。

## v0.3.0（プレゼンHTML・発表者ビュー対応）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。`window.open()`で開くプレゼン専用ウィンドウは
Playwrightの`page.waitForEvent('popup')`で捕捉し、独立した`Page`として直接操作・検証した。
HTML出力は実際にダウンロードしたファイルを`file://`で開いて検証した。

### 実装方針の要点

- `slide-preview.js`からプレゼン関連（別ウィンドウ全画面化・キー処理・現在スライド管理）を
  すべて削除し、`presenter.js`（新規）へ移した。`slide-preview.js`はMarp初期化・レンダリング・
  編集時プレビュー・テーマ・HTML出力・印刷のみを担当する。
- スピーカーノートは独自解析を実装せず、Marpit公式のPresenter notes機能
  （`marp.render(markdown).comments`、`string[][]`、ディレクティブ用YAMLを除いたHTMLコメント）を
  そのまま利用した。
- iframe（編集時プレビュー・発表者ビューの現在/次スライド）とプレゼン専用ウィンドウは、
  同一のブートストラップ用HTML/JS（`slide-preview.js`の`buildFrameDocument()`）を共用し、
  `postMessage`の送信先（`parent`または`window.opener`）だけを切り替えている。
- ウィンドウ間の状態同期は、本体（`presenter.js`）が現在スライド番号を一元管理し、
  発表者ビューの前後ボタン・プレゼン専用ウィンドウのキー操作のどちらも「本体へ移動を要求する
  メッセージ」を送るだけとした（各表示先が自分で勝手にページを進めることはない）。本体が
  `goto`メッセージを全表示先へ配信することで、表示先間の食い違いを防いでいる。

### プレゼン専用ウィンドウ

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 「プレゼン表示」で別ウィンドウが開く | OK |
| 2 | MarkdownUtil本体は残る | OK（`page.isClosed()`がfalseのまま） |
| 3 | プレゼン側にはスライドのみ表示 | OK（本体のボタン文字列「ファイルを開く」等が含まれないことを確認） |
| 4 | 前後操作できる | OK |
| 5 | Home / End | OK |
| 6 | 全画面化できる | OK（ウィンドウ右下に「⛶ 全画面」ボタンが表示されることを確認。実際のOSレベルの全画面遷移はheadless環境の制約上、ボタンの存在とクリック可能性まで確認） |
| 7 | ウィンドウを閉じても本体が壊れない | OK（ポップアップを閉じてから約0.8秒後、本体のツールバーが復帰し、編集内容も保持されていることを確認） |

### 発表者ビュー

| # | 内容 | 結果 |
| --- | --- | --- |
| 8 | 現在スライド表示 | OK |
| 9 | 次スライド表示 | OK |
| 10 | 現在位置表示 | OK（`1 / 3`のように表示） |
| 11 | 前へ／次へ | OK |
| 12 | スピーカーノート表示 | OK |
| 13 | ノート無しスライド | OK（「スピーカーノートはありません」と表示） |
| 14 | Marpディレクティブをノートとして誤表示しない | OK（`<!-- _class: lead -->`を挿入したスライドのノート表示に`_class`という文字列が含まれないことを確認。Marpit自体がディレクティブ用コメントを`comments`から除外している） |
| 15 | 最終スライドでは「次」の表示を適切に処理 | OK（「次」欄が「これが最後のスライドです」というプレースホルダー表示に切り替わり、「次へ」ボタンが無効化される） |

### 同期

| # | 内容 | 結果 |
| --- | --- | --- |
| 16 | 発表者ビュー操作 → プレゼン画面へ反映 | OK |
| 17 | プレゼン画面操作 → 発表者ビューへ反映 | OK（ポップアップ側で`ArrowRight`を押し、本体の位置表示・ノートが追従することを確認） |
| 18 | 高速に前後移動してもずれない | OK（前へ×2、次へ×2、前へを連続実行後、本体とポップアップの表示スライドが一致することを確認） |
| 19 | プレゼンウィンドウを途中で閉じても例外にならない | OK（consoleエラー0件のまま本体が復帰） |

状態整合性（v0.2.4までの制御）についても確認した。編集直後、プレビュー更新のdebounce
（150ms）完了前に「プレゼン表示」を押した場合、ポップアップは開かれず
「現在のMarkdownがまだスライドへ反映されていません。」と表示されることを確認した
（`isRenderCurrent()`によるチェックが、HTML出力・印刷と同じく「プレゼン表示」にも
適用されている）。

### HTML出力（スライドショー対応）

| # | 内容 | 結果 |
| --- | --- | --- |
| 20 | HTML単体で開ける | OK（`file://`で直接開いて確認） |
| 21 | 1枚ずつスライド表示 | OK（開いた直後、対象スライドのSVGのみ`display:block`、他2枚は`none`であることを確認） |
| 22 | キーボード操作 | OK（`→` `End` `Home`で位置表示が追従） |
| 23 | クリック操作 | OK（スライド領域のクリックで次へ進む） |
| 24 | 現在位置表示 | OK |
| 25 | 全画面 | OK（全画面ボタンの存在を確認） |
| 26 | ノート情報保持 | OK（出力HTMLに埋め込まれたノート文字列を確認。既定では非表示で、「ノート」ボタンまたは`N`キーで表示・再度非表示にできることも確認） |
| 27 | 印刷 | OK（印刷用ウィンドウを`emulateMedia({media:'print'})`で検証。ナビゲーションバー・ノートパネルは`display:none`、各スライドSVGは`display:block`かつ`break-after:page`でページ分割される設定を確認。v0.2.0時点の印刷CSSと同じ1枚1ページの挙動が維持されている） |
| 28 | 外部通信0件 | OK（`file://`で開いた状態でのリクエストを監視し、`file://`以外への通信が0件であることを確認） |

### 回帰

| # | 内容 | 結果 |
| --- | --- | --- |
| 29 | AnyDoc変換 | OK（DOCX→Markdown変換、ステータス表示とも従来どおり） |
| 30 | 通常Markdownプレビュー | OK |
| 31 | Marp通常プレビュー | OK（編集時プレビューiframeでの表・箇条書き・コードブロック表示を確認） |
| 32 | Markdown保存 | OK |
| 33 | HTML出力（正常系） | OK |
| 34 | 印刷（正常系） | OK |
| 35 | テーマ（default/gaia/uncover、gaiaのリモート`@import`除去） | OK |
| 36 | v0.2.4の状態整合性制御（debounce待機中・モード切替直後のブロック） | OK |

v0.1.0〜v0.2.4で作成した既存のテストスクリプトをすべて再実行し、`#presenter-hint`要素の
参照（v0.3.0で発表者ビューへ置き換えたため存在しない）を除き、すべて期待どおりの結果と
なることを確認した。ドラッグ&ドロップ・コピー・クリア・スライドモードでのツールバー
表示制御（v0.2.1の修正）についても回帰がないことを確認した。

### v0.3.0まとめ

プレゼン表示を「本体の全画面化」から「投影用の別ウィンドウ＋手元の発表者ビュー」という
実際の会議で使える構成へ変更した。ウィンドウ間の同期（前後移動・Home/End・キーボード）、
スピーカーノートの正しい取得（ディレクティブとの区別を含む）、最終スライドの表示、
ポップアップが閉じられた場合の復帰、状態整合性チェックの継続適用を確認した。あわせて
HTML出力を縦一覧表示から単体スライドショーへ変更し、キーボード・クリック・全画面・
ノート表示切替・印刷（1枚1ページ）が外部通信なしに動作することを確認した。v0.1.0〜v0.2.4の
機能に回帰がないことも確認済み。

### 既知の制限（v0.3.0で追加）

- プレゼン専用ウィンドウの拡張ディスプレイへの自動移動・自動全画面化は行わない（庁内IISの
  HTTP配信を前提に、Window Management API等は使用していない）。ウィンドウの移動・全画面化は
  利用者の手動操作が必要。
- プレゼン中はMarkdownの編集ができない（発表者ビューに切り替わると編集領域が隠れる）。
  投影中の内容と編集中の内容の食い違いを避けるための単純化であり、意図的な制限である。
- プレゼン専用ウィンドウはブラウザのポップアップブロックの対象になる。ブロックされた場合は
  「プレゼン用ウィンドウを開けませんでした。ポップアップの許可を確認してください。」と表示する。
- ポップアップが閉じられたことの検知は、`closed`プロパティを一定間隔（0.4秒）で確認する方式。
  そのため、閉じてから本体が発表者ビューを終了するまでに最大0.4秒程度のタイムラグがある。
- HTML出力のスライドショーは、MarkdownUtil本体の発表者ビュー（現在/次スライドの並列表示・
  別ウィンドウとの同期）ほど高機能ではない。PowerPoint互換の発表者ツールを目指したものでは
  なく、出力先単体で最低限スライドショーできることを優先している。

## v0.3.1（プレゼン全画面操作の回帰修正）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。新機能は追加せず、`js/presenter.js`のバグを1件修正した。

### 修正した問題

プレゼン専用ウィンドウ右下の「⛶ 全画面」ボタンに`stopPropagation()`が無かったため、
クリックがドキュメント全体のクリックリスナー（クリックで次のスライドへ進む処理）へ伝播し、
**全画面化と同時にスライドが1枚進んでしまっていた**。実際に3枚のMarkdownでプレゼンを開始し
（1/3）、全画面ボタンを押すと位置表示が2/3になることを確認した。「外部ディスプレイへ移動 →
全画面ボタン → 発表開始」という想定利用フローの入口で必ず発生する不具合だった。

`mu-fullscreen-btn`のクリックハンドラの先頭で`event.stopPropagation()`を呼ぶよう修正し、
全画面化してもスライドの位置が変わらないことを確認した。あわせて、`:fullscreen
.mu-fullscreen-btn { display: none; }`を追加し、全画面化した後は投影内容がスライドだけに
なるようにした（Escキーで全画面を解除できるため、ボタンを常に表示しておく必要は薄いと判断）。

同種の問題（ボタンのクリックがスライド送りへ伝播する）が他に無いかを確認したところ、
HTML出力のスライドショー（`slide-preview.js`の`buildStandaloneHtml()`）側は元から
`ss-next` / `ss-prev` / `ss-notes-toggle` / `ss-fullscreen`すべてに`stopPropagation()`が
入っており、影響は無かった。

### 追加確認したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 1/3でプレゼン開始 | OK |
| 2 | 全画面ボタンをクリック | OK |
| 3 | クリック後も位置が1/3のまま（修正前は2/3になっていた） | OK |
| 4 | 全画面時に`.mu-fullscreen-btn`を隠すCSSルールが存在する | OK |
| 5 | 発表者ビューの「次へ」ボタンによる通常の移動は引き続き機能する | OK |
| 6 | プレゼン専用ウィンドウのスライド本体をクリックした場合の「次へ」進む挙動は維持される（全画面ボタン以外のクリックまで止めていないことの確認） | OK |

v0.2.4までの状態整合性チェック、v0.3.0のプレゼン専用ウィンドウ・発表者ビュー・同期・
HTML出力・印刷の各テストを再実行し、回帰がないことを確認した。外部通信0件、
consoleエラー0件。

### v0.3.1まとめ

プレゼン専用ウィンドウの「全画面」ボタンが、意図せずスライドを1枚進めてしまう不具合を
修正した。原因はクリックイベントの伝播を止めていなかったことで、`stopPropagation()`の
追加で解決した。あわせて全画面時にボタン自体を隠し、投影内容をスライドだけにした。
変更は`js/presenter.js`の1箇所（クリックハンドラとCSS）のみ。

## v0.4.0（編集支援ヘルプ: Markdown / Marp 早見表）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。既存の回帰テストスクリプト（v0.1.0〜v0.3.1分）も
すべて再実行した。

### 実装方針の要点

- `js/help.js`（新規）を追加し、早見表データ（名称・簡単な説明・記法例の配列）、
  モーダルの開閉・タブ切替・記法例のクリップボードコピーをここへ集約した。`app.js`は
  DOM要素の受け渡しとボタンの配線のみ（早見表データやUI生成コードは持ち込んでいない）。
  `converter.js` / `preview.js` / `slide-preview.js` / `presenter.js`は変更していない。
- 記法例はすべて`textContent`でDOMへ設定しており、`innerHTML`は使用していない。
  Markdown/MarpのコードフェンスやHTMLコメントを含む記法例（例:
  `` ```text ... ``` ``、`<!-- _class: lead -->`）がそのままエスケープされずに文字列として
  表示され、実行やレンダリングの対象にならないことを確認した。
- コピー機能は`app.js`のコピー処理と同じ実装パターン（Clipboard API、失敗時は
  `execCommand('copy')`にフォールバック）を`help.js`内に持ち、コピー結果は`app.js`から
  渡された`onStatus`コールバック経由で既存のステータス表示へ出す（独自の通知UIは追加していない）。
- モーダルの開閉は`hidden`属性で行い、v0.2.1の教訓（`display`を無条件指定すると`hidden`属性が
  上書きされる）を踏まえて`.help-overlay:not([hidden]) { display: flex; }`とした。

### テスト結果

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | ヘルプボタンで早見表が開く | OK（`hidden`属性が外れ、`computed display`が`flex`になることを確認） |
| 2 | Markdown / Marpを切り替えられる | OK（タブの`is-active`・`aria-selected`、対応するパネルの表示/非表示が連動） |
| 3 | 各主要記法が表示される | OK（Markdown10項目・Marp9項目、要求された項目名がすべて含まれることを確認） |
| 4 | コード例がMarkdownとして崩れず表示される | OK（表の記法例・コードフェンス記法例が、エスケープや欠落なく`<code>`のテキストとして取得できることを確認） |
| 5 | コピー操作が正常に動く | OK（「表」の記法例をコピーし、クリップボードの内容が記法例と一致することを確認） |
| 6 | コピー後もMarkdown本文を変更しない | OK（コピー前後でエディタの内容が完全一致） |
| 7 | 閉じるボタンで閉じられる | OK（閉じた後、フォーカスがヘルプボタンへ戻ることも確認） |
| 8 | `Esc`で閉じられる | OK |
| 9 | 再度開ける | OK |
| 10 | ヘルプ開閉後もエディタ内容が保持される | OK |
| 11 | 小さい画面でもヘルプ内部をスクロールできる | OK（幅380pxの画面で`.help-body`の`scrollHeight`が`clientHeight`を上回り、実際に`scrollTop`を変更できることを確認） |
| 12 | 通常画面にヘルプが常駐しない | OK（閉じている間は`computed display`が`none`） |
| 13 | 既存v0.1.0〜v0.3.1機能に回帰なし | OK（後述） |
| 14 | 外部通信0件 | OK |
| 15 | consoleエラー0件 | OK |

上記に加え、背景（オーバーレイ自身）のクリックで閉じること、ダイアログ内をクリックしても
閉じないこと（誤操作対策）も確認した。

### 回帰確認（v0.1.0〜v0.3.1）

既存のテストスクリプトをすべて再実行し、回帰がないことを確認した。

- AnyDoc変換・通常Markdownプレビュー・コピー・保存・ドラッグ&ドロップ（v0.1.0）
- スライドモードへの切替、テーマ変更（default/gaia/uncoverとgaiaのリモート`@import`除去）、
  文書モードでのスライド専用ツールバー非表示（v0.2.0、v0.2.1）
- 状態整合性チェック（解析エラー時・debounce待機中・モード切替直後のHTML出力/印刷/
  プレゼン表示のブロック）（v0.2.3、v0.2.4）
- プレゼン専用ウィンドウの開閉・発表者ビュー・双方向同期、HTML出力の単体スライドショー
  （キーボード・クリック・全画面・ノート表示切替・印刷）（v0.3.0）
- プレゼン専用ウィンドウの全画面ボタンでスライドが進まないこと（v0.3.1）

いずれも期待どおりの結果で、外部通信0件・consoleエラー0件だった。

### v0.4.0まとめ

Markdown編集領域に「? ヘルプ」ボタンと、Markdown / Marpの記法早見表（タブ切替・記法例の
コピー）を追加した。早見表は`js/help.js`へ分離し、`app.js`の肥大化を避けた。記法例は
すべて`textContent`で扱っているためMarkdownとして解釈されたり崩れたりすることはなく、
コピー操作もエディタの内容に影響しない。閉じるボタン・`Esc`・背景クリックのいずれでも
閉じられ、閉じた後はヘルプボタンへフォーカスが戻る。既存のAnyDoc変換・スライド機能・
プレゼン機能・状態整合性制御に回帰がないことを確認した。

### 既知の制限（v0.4.0で追加）

- 早見表はMarkdown/Marpの代表的な記法の一覧であり、仕様を網羅した解説ではない。
- コピーした記法例をエディタのカーソル位置へ自動挿入する機能はない（貼り付けは手動）。
- 早見表の内容はMarkdownUtil側に固定されており、利用者が項目を追加・編集する機能はない。
- 初回起動時にヘルプを強制表示する機能はない（利用者が「? ヘルプ」を押した場合のみ表示）。

## v0.4.1（ヘルプのキーボード操作・コピー後フォーカス改善）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。新機能は追加せず、`js/help.js`のキーボード・
フォーカス関連の不具合を修正した。

### 修正した問題

**1. Tab/Shift+Tabでダイアログの外（背面の通常画面）へ抜けられる**

`role="dialog" aria-modal="true"`を名乗っていたが、`keydown`ではEscapeしか処理しておらず、
Tabキーの移動先を制御していなかった。実際に確認したところ、ヘルプを開いた状態で`Tab`を
13回押すとダイアログ外へフォーカスが抜け、`Shift+Tab`は1回目で背面の「スライド」タブ
ボタンへ直接フォーカスが移ってしまうことを確認した。見た目はモーダルなのに、キーボード
操作だけで背面のUIを操作できてしまう状態だった。

**2. `execCommand('copy')`フォールバック後、フォーカスがコピー元ボタンへ戻らない**

Clipboard APIが使えない環境（HTTP配信のIIS等でよくある）では一時`<textarea>`へ`focus()`して
コピーする経路（`legacyCopy()`）を通るが、コピー後にその一時要素を削除するだけでフォーカスを
戻していなかった。実際に確認したところ、コピー後のフォーカスが`<body>`に落ちることを確認した。

### 修正

- ダイアログ内の実際に見えている（隠れているタブパネル内ではない）フォーカス可能要素を
  取得し、`Tab`で末尾から先頭へ、`Shift+Tab`で先頭から末尾へ折り返す`trapTab()`を追加した。
- `copySnippet()`にコピー元のボタン要素を渡すようにし、`finally`節で
  （Clipboard API経路・`legacyCopy()`経路のいずれでも）コピー元のボタンへフォーカスを
  戻すようにした。
- `Esc`で閉じた場合の挙動（「? ヘルプ」ボタンへフォーカスを戻す）は既存のまま変更していない。

あわせて、レビューで軽微なUXとして挙げられた「コピー成功通知がオーバーレイの背面にあり
気づきにくい」点について、コピー元のボタン自体を一時的に「コピー済み」表示へ切り替える
`flashCopied()`を追加した（1.2秒後に元の表示へ戻る）。既存のステータス表示による通知は
そのまま維持しており、独自の大きな通知UIは追加していない。

### 追加確認したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | ヘルプを開いた状態で`Tab`を40回押してもダイアログの外へ出ない | OK（修正前は13回目で脱出していた） |
| 2 | 同様に`Shift+Tab`を40回押してもダイアログの外へ出ない | OK（修正前は1回目で脱出していた） |
| 3 | Clipboard APIを使えない環境を模擬し、コピー後にコピー元のボタンへフォーカスが戻る | OK（修正前は`<body>`に落ちていた） |
| 4 | コピー直後、ボタンの表示が「コピー済み」に変わり、約1.2秒後に「コピー」へ戻る | OK |
| 5 | コピー後もフォーカスがそのボタン上に維持される | OK |
| 6 | `Esc`で閉じると「? ヘルプ」ボタンへフォーカスが戻る（従来どおり） | OK |
| 7 | 同じボタンを表示切り替え中に連続でクリックしても、最終的に元の表示へ戻る（タイマー競合なし） | OK |

v0.4.0のヘルプ機能テスト（開閉3通り・タブ切替・記法表示・コピー・小画面スクロール等）、
v0.3.1までのプレゼン関連テストを再実行し、回帰がないことを確認した。外部通信0件、
consoleエラー0件。

### v0.4.1まとめ

ヘルプモーダルがキーボード操作でも実際にモーダルとして振る舞うよう、Tab/Shift+Tabを
ダイアログ内で循環させるフォーカストラップを追加した。また、Clipboard APIが使えない
環境でのコピー操作後にフォーカスが失われる問題を修正し、コピー元のボタンへ確実に
戻るようにした。あわせてコピー成功をボタン自体の一時的な表示変化でも分かるようにした。
変更は`js/help.js`のみで、他のモジュールには影響しない。

## v0.4.2（ヘルプの利用ガイド追加）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。ヘルプモーダルを「Markdown / Marp 早見表」の
2タブ構成から、「使い方」「Markdown」「Marp」「プレゼン」の4タブ構成へ拡張した。
初回オープン時に「使い方」タブを既定表示とし、MarkdownUtil自体の使い方とプレゼン
機能の使い方を、README等の外部資料を開かなくてもアプリ内で把握できるようにした。

### 追加した内容

**使い方タブ**: 冒頭の要約文、「できること」一覧（8項目）、「基本的な利用の流れ」
（5ステップ）、CSSのみで組んだ簡易フロー図（Office文書/PDF → Markdown →
文書表示/スライド表示への分岐）、「読み込めるファイル」の対応形式一覧と、
画像のみのスキャンPDFは変換できない場合がある旨の注記、「文書表示とスライド表示の
違い」の説明（定義リスト形式）。

**プレゼンタブ**: プレゼン表示（外部ディスプレイ利用）の4ステップ手順と、
自動移動・自動全画面化は行わない方針（HTTP配信のIIS環境を想定）の明記、
発表者ビューの機能一覧、スピーカーノートの簡単な例（既存のコピー可能な記法例と
同じUIを再利用し、詳細はMarpタブを参照する旨を明記）、HTML出力の簡単な要約。

いずれも「その場で読める要約」にとどめ、README相当の詳細な解説やチュートリアル、
画像・動画、外部マニュアルへのリンクは追加していない。

### 実装

- `js/help.js`: `MARKDOWN_ITEMS`/`MARP_ITEMS`はそのまま維持し、`GUIDE_FEATURES`
  `GUIDE_STEPS`等の新しいデータ定数と、`buildGuidePanel()`/`buildPresentationPanel()`を
  追加した。記法例1件分のDOM構築は`buildItem()`として`buildPanel()`から切り出し、
  プレゼンタブのスピーカーノート例でも再利用した（コピー機能・「コピー済み」表示・
  フォーカス復帰の挙動を完全に共有するため）。
- `setTab()`/`renderPanelsOnce()`/`init()`を、`markdown`/`marp`の2値ハードコードから
  `TAB_NAMES = ['guide', 'markdown', 'marp', 'presentation']`を単一の情報源とする
  実装へ一般化した。`open()`の既定タブは`'markdown'`から`'guide'`へ変更した。
  `focusableElements()`/`trapTab()`/`copySnippet()`/`flashCopied()`等の既存ロジックは
  変更していない。
- `index.html`: タブボタンとパネルをそれぞれ2つ追加し（`help-panel-markdown`/
  `help-panel-marp`は既定で`hidden`、`help-panel-guide`が既定で表示）、ヘルプの
  タイトルを「Markdown / Marp 早見表」から「MarkdownUtil ヘルプ」に変更した。
- `css/app.css`: 使い方・プレゼンタブの文章コンテンツ用のスタイル
  （`.help-guide__*`、`.help-section__title`、フロー図用の`.help-flow__*`）を追加し、
  4タブが狭い画面でも折り返せるよう`.help-tabs`に`flex-wrap: wrap`を加えた。
  既存の`.help-item`系のスタイルは変更していない。
- `js/app.js`は新しいDOM参照4つの取得と`help.init()`への引き渡しのみを追加し、
  ガイド・プレゼンタブの文言や構造は一切持ち込んでいない（要求どおり`help.js`側に集約）。
- `converter.js`/`preview.js`/`slide-preview.js`/`presenter.js`は変更していない。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | ヘルプを開いた直後、既定で「使い方」タブが選択されている（`is-active`/`aria-selected`/パネル表示） | OK |
| 2 | 「使い方」タブに「できること」「基本的な利用の流れ」「読み込めるファイル」「文書表示とスライド表示の違い」の見出しが存在する | OK |
| 3 | 「使い方」タブにスキャンPDFに関する注記が含まれる | OK |
| 4 | フロー図に4つのボックス（Office文書/PDF・Markdown・文書表示・スライド表示）が表示される | OK |
| 5 | 4タブすべてを順に切り替え、選択中以外のパネルが`hidden`属性・`display:none`になる | OK |
| 6 | 「プレゼン」タブに「プレゼン表示」「発表者ビュー」「スピーカーノート」「HTML出力」の内容と、自動移動しない旨の記述が含まれる | OK |
| 7 | プレゼンタブのスピーカーノート例にもコピー可能な記法例UIがあり、コピー操作が機能する | OK |
| 8 | Markdown/Marpタブの既存の早見表項目数（10件/9件）が変わっていない | OK |
| 9 | 既存の「コピー済み」フラッシュ表示（約1.2秒で元に戻る）が引き続き機能する | OK |
| 10 | `Tab`を60回押してもダイアログ外へ脱出しない（4タブ構成でもフォーカストラップが機能する） | OK |
| 11 | `Shift+Tab`を60回押しても脱出しない | OK |
| 12 | 非表示タブのパネルには`hidden`属性が付き、そのパネル内のボタンは`Tab`で拾われない | OK |
| 13 | `Esc`で閉じると「? ヘルプ」ボタンへフォーカスが戻る | OK |
| 14 | 再度開くと、直前に選択していたタブに関わらず「使い方」タブから表示される | OK |
| 15 | 背景（オーバーレイ）クリックで閉じ、ダイアログ内クリックでは閉じない | OK |
| 16 | 幅380pxの狭い画面でタブが折り返し、本文がモーダル内スクロールに収まる | OK |
| 17 | Markdownタブへ切り替えたうえでの記法コピー（クリップボード反映・フォーカス復帰）が機能する | OK |
| 18 | ヘルプ利用中もコンソールエラーが発生しない | OK |
| 19 | ヘルプ利用中も外部へのHTTPリクエストが発生しない | OK |

あわせて、v0.1.0〜v0.4.1の回帰テスト一式（AnyDoc変換、XSS対策、文書/スライド表示切替、
テーマ切替、HTML出力、印刷、プレゼンポップアップ・発表者ビュー・同期、全画面ボタンの
伝播対策、状態整合性（`isRenderCurrent`）、Marp読込失敗時のフォールバック、既存の
Markdown/Marp早見表の開閉・コピー）を再実行し、回帰がないことを確認した。外部通信0件、
consoleエラー0件。

なお、v0.3.0以前（発表者ビュー導入前）の一部の古いテストスクリプトは、当時のUI要素
（`#presenter-hint`）を参照しており、v0.3.0でのUI刷新以降は該当要素が存在しないため
タイムアウトする。これはv0.4.2でのリグレッションではなく、v0.3.0の時点で構造が変わった
ことによる既存スクリプト側の陳腐化であり、現行構成に対応したv0.3.0以降のスクリプト
（発表者ビュー・ポップアップ・HTML出力・印刷を検証するもの）ではすべてOKだった。
同様に、ヘルプの既定タブが「使い方」に変わったことで、既定タブを「Markdown」と
決め打ちしていたv0.4.0/v0.4.1当時のスクリプト2本もタブ切替なしでは要素を見つけられ
なくなるが、これは仕様どおりの変更であり、明示的にMarkdownタブへ切り替えたうえでの
再確認（#17）でコピー機能に問題がないことを確認済み。

### v0.4.2まとめ

ヘルプモーダルを記法早見表専用から、MarkdownUtil自体の使い方とプレゼン機能の使い方を
含む簡易ヘルプへ拡張した。初めて使う職員でも、README等を別途開かずにアプリ内の
「使い方」「プレゼン」タブを読むだけで、変換から編集・表示・保存までの基本操作と、
プレゼン表示（外部ディスプレイ利用）の流れを把握できる。既存のMarkdown/Marp早見表・
コピー機能・フォーカストラップ・キーボード操作は`js/help.js`内のロジック共通化
（`buildItem()`の抽出、`TAB_NAMES`による一般化）によってそのまま維持しており、
`app.js`にはDOM参照の配線のみを追加した。カード型UIやアイコン装飾、画像・動画、
検索機能、AIチャット等は追加せず、既存の簡素な見た目のトーンを踏襲している。

### 既知の制限（v0.4.2で追加）

- 「使い方」「プレゼン」タブの内容は要点の要約であり、README記載の詳細な仕様や
  既知の制限のすべてを網羅するものではない。
- 初回起動時にヘルプを強制表示する機能や、操作をなぞらせるステップ形式のチュートリアル、
  動画・アニメーションによる説明はない（利用者が「? ヘルプ」を押した場合のみ表示）。
- ヘルプ内に検索機能やAIによる質問応答はない。
- ガイド内の文言はMarkdownUtil側に固定されており、利用者が項目を追加・編集する機能はない。

## v0.4.3（利用ガイド文言・対応形式の整合修正）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。v0.4.2でマージされたヘルプの「使い方」「プレゼン」
タブについて、PR #11のレビューで指摘された4点の文言修正を行った。ロジック変更はなく、
`js/help.js`内のデータ定数（`GUIDE_FEATURES`/`GUIDE_STEPS`/`GUIDE_FILE_TYPES`/
`GUIDE_DOC_VS_SLIDE`/`PRESENTATION_STEPS`/`HTML_EXPORT_NOTE`等）と、`buildGuidePanel()`/
`buildPresentationPanel()`内の文字列のみを修正した。

### 修正した4点

**1. 実際のUI名称との不一致**

ヘルプ本文が「ファイルを選択」「中央のエディタ」「文書表示」「スライド表示」
「プレゼン開始」と表記していたが、実際の画面は「ファイルを開く」ボタン、
（デスクトップでは）左側のエディタ、「文書」「スライド」タブ、「プレゼン表示」ボタン
である。特に「プレゼン開始」は実在しないボタン名を利用者が探してしまう問題があった。
実際のラベル・配置に合わせて本文を修正した（`index.html`のボタン・タブラベル自体は
変更していない）。

**2. 「読み込めるファイル」の記載不足**

Word / Excel / PowerPoint / PDF / Markdownのみを記載していたが、実際の
`PICKER_EXTENSIONS_HINT`（`js/converter.js`）にはCSV / RTF / EPUB / ODT / ODS / ODPも
含まれている。これらを追加したうえで、「実際に変換できるかどうかは読み込み時にAnyDocが
判定する」という留保を明記し、「代表的な形式」という表現にとどめた（`converter.js`が
一覧をUI上の目安に過ぎないとしている方針と整合させるため）。

**3. 「外部通信は行わない」という断定的な表現**

使い方タブ冒頭が「すべてブラウザ内で完結し、外部通信は行わない」と断定していたが、
README記載のとおり、編集中のMarkdownに外部URL画像等を書いた場合はブラウザによる
外部通信が発生しうる。「文書変換・編集・表示はブラウザ内で処理し、MarkdownUtil自身は
外部APIやCDNへ通信しない」という、MarkdownUtil自身の挙動に限定した表現へ修正した。

**4. HTML出力の説明が実際の仕組みと異なる**

「Marpを使わずそのまま開ける」としていたが、実際にはMarkdownUtil内でMarp Coreにより
レンダリングした結果を使ってHTMLを生成し、出力するHTMLファイル自体にはMarp Coreを
同梱しない、という仕組み（`js/slide-preview.js`の`buildStandaloneHtml()`）である。
「Marp Coreを同梱せず、単体でスライドショーできるHTMLファイルとして保存できる」という
表現へ修正した。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 「使い方」タブに「ファイルを開く」を含み、旧「ファイルを選択」を含まない | OK |
| 2 | 「使い方」タブに「左側」を含み、旧「中央のエディタ」を含まない | OK |
| 3 | 「使い方」タブが「文書」「スライド」タブ表記になり、「文書表示」「スライド表示」という表現を含まない | OK |
| 4 | 「プレゼン」タブに「プレゼン表示」を含み、旧「プレゼン開始」を含まない | OK |
| 5 | 「読み込めるファイル」にWord/Excel/PowerPoint/PDF/CSV/RTF/OpenDocument/EPUB/Markdownをすべて含む | OK |
| 6 | 「読み込めるファイル」に「代表的な形式」という留保表現を含む | OK |
| 7 | 使い方タブ冒頭が、旧「すべてブラウザ内で完結し、外部通信は行わない」を含まない | OK |
| 8 | 使い方タブ冒頭が「MarkdownUtil自身は外部APIやCDNへ通信しない」という限定表現を含む | OK |
| 9 | プレゼンタブのHTML出力説明が「Marp Coreを同梱せず」を含み、旧「Marpを使わずそのまま開ける」を含まない | OK |
| 10 | Markdown/Marpタブの早見表項目数（10件/9件）が変わっていない（回帰） | OK |
| 11 | コピー操作・「コピー済み」表示が引き続き機能する（回帰） | OK |
| 12 | 4タブ構成でのTabフォーカストラップが引き続き機能する（回帰） | OK |

あわせて、v0.3.0〜v0.4.2の主要な回帰テスト（ヘルプ開閉・タブ切替・プレゼンポップアップ・
発表者ビュー・HTML出力・状態整合性）を再実行し、回帰がないことを確認した。外部通信0件、
consoleエラー0件。

### v0.4.3まとめ

v0.4.2で追加した利用ガイドの内容について、実UIとの名称不一致・対応形式の記載漏れ・
外部通信に関する断定的すぎる表現・HTML出力の仕組みの誤解を招く表現の4点を修正した。
いずれも`js/help.js`内の文言（データ定数と本文組み立て関数）のみの修正であり、
タブ構成・フォーカストラップ・コピー機能などv0.4.2で実装した構造・ロジックへの変更は
ない。`index.html`のボタン・タブラベル自体（実UI側の表記）も変更していない。

### 既知の制限（v0.4.3で追加）

- なし（v0.4.2の既知の制限がそのまま引き続き適用される）。

## v0.5.0（文書・スライドサンプルの挿入）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認（`python3 -m http.server`で配信、コンソールエラーと
ネットワークログを監視）。「使い方」タブの末尾に「サンプルで試す」を追加し、「文書サンプルを
開く」「スライドサンプルを開く」から、それぞれMarkdownUtilの「文書」「スライド」機能を
紹介するチュートリアルMarkdownをエディタへ挿入できるようにした。単なるMarkdown記法集では
なく、MarkdownUtil自身の基本操作（編集・プレビュー・保存・コピー・ヘルプ）を文中で説明する
文章・スライドとした。

### 追加した内容

**文書サンプル**（`js/samples.js`の`DOCUMENT_SAMPLE`）: 「# MarkdownUtil「文書」の紹介」を
タイトルに、MarkdownUtilの説明、基本操作（左側の編集・右側の「文書」タブでの確認・保存・
コピー・ヘルプ）、見出し・太字・斜体・番号付きリスト・箇条書き・表・引用を自然な文章の中で
使った例、末尾に自由に書き換えて連動を試すよう促す一文を含む。

**スライドサンプル**（`js/samples.js`の`SLIDE_SAMPLE`）: `marp: true` / `theme: default` /
`paginate: true`のfront matter、タイトルスライド、基本操作（左側で編集・右側の「スライド」
タブで確認・`---`区切り・Markdown記法がそのまま使える）、見出し・太字・箇条書き・表を使った
スライド、テーマ・HTML出力・印刷・プレゼン表示・スピーカーノートを紹介する表、タイトル
スライドに実際のスピーカーノート（HTMLコメント）を1件含め、最終スライドで「左側の
Markdownを書き換えてスライドが変化することを試す」「ヘルプ→Marpタブで詳しい記法を確認」
旨を表示する構成とした。

### 実装

- `js/samples.js`（新規）: `DOCUMENT_SAMPLE` / `SLIDE_SAMPLE`の2つのMarkdown文字列のみを
  export する。サンプル本文が2件にとどまるため、`app.js`/`help.js`とは分離しつつも
  過剰なディレクトリ分割は行っていない。
- `js/help.js`: `buildGuidePanel()`の末尾に「サンプルで試す」の見出し・説明文・2つの
  ボタン（`help-guide__sample-btn`）を追加した。ボタンのクリックは`els.onInsertDocumentSample`
  /`els.onInsertSlideSample`という**通知のみ**のコールバックを呼ぶだけで、サンプル本文の
  保持やエディタ・Marpへの反映ロジックは一切持ち込んでいない（`help.js`はエディタ/Marpの
  内部処理に依存しない、という既存の責務分離を維持）。`init()`のJSDocと引数にも
  この2コールバックを追加した。
- `js/app.js`: `samples.js`から`DOCUMENT_SAMPLE`/`SLIDE_SAMPLE`をimportし、
  `insertSample(sample, mode, filename, confirmLabel, successMessage)`という共通関数を
  追加した。中身は`editor.value`への代入 → `applyModeUI(mode)` →
  `refreshActivePreview()`という、既存の`handleFile()`・`clearAll()`と同じ経路を再利用する
  だけで、プレビュー更新・スライド描画・`isRenderCurrent()`用の状態記録を個別に
  呼び直すコードは書いていない。上書き確認は、編集中に空でなければ既存の`clearAll()`と
  同様に`window.confirm()`を使う（新しいモーダル基盤は追加していない）。挿入後は
  `help.close()`でヘルプを閉じ、`editor.focus()`でフォーカスをエディタへ戻す。
  `help.init()`呼び出しに`onInsertDocumentSample`/`onInsertSlideSample`を渡す2行のみを
  追加し、サンプル本文やUI文言は持ち込んでいない。
- `css/app.css`: 「サンプルで試す」ボタン用に`.help-guide__sample-actions`/
  `.help-guide__sample-btn`を追加した。既存の`.toolbar-actions button`と同系統の
  見た目（枠線・角丸・ホバー/アクティブ/フォーカス表示）にそろえ、カード化や
  アイコン装飾は行っていない。上部ツールバーには新しいボタンを追加していない。
- `converter.js`/`preview.js`/`slide-preview.js`/`presenter.js`は変更していない。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | エディタが空の状態で「文書サンプルを開く」を押すと、確認なしでサンプルが挿入される | OK |
| 2 | 挿入後、エディタに文書サンプルの内容（タイトル・基本操作・記法例・表・引用等）が入る | OK |
| 3 | 挿入後、右側のプレビューが自動的に「文書」タブへ切り替わる（既に「文書」の場合も含む） | OK |
| 4 | 文書プレビューに見出し・箇条書き・番号付きリスト・表・引用・太字・斜体が表示される | OK |
| 5 | 挿入後、ヘルプが閉じる | OK |
| 6 | 挿入後にエディタへ手動で文字を入力すると、右側の文書プレビューへ反映される（debounce経由） | OK |
| 7 | エディタが空の状態で「スライドサンプルを開く」を押すと、確認なしで挿入される | OK |
| 8 | 挿入したMarkdownに`marp: true`のfront matterが含まれる | OK |
| 9 | 挿入後、複数枚（5枚）のスライドとして描画される（スライドツールバーに「5枚」表示） | OK |
| 10 | 挿入後、右側のプレビューが自動的に「スライド」タブへ切り替わる | OK |
| 11 | 挿入したMarkdownに実際のスピーカーノート（HTMLコメント）が含まれる | OK |
| 12 | プレゼン表示を開くと、発表者ビューの「スピーカーノート」欄にサンプルのノート文が表示される | OK |
| 13 | スライドサンプル挿入直後に「HTML出力」を押すと、サンプル内容を反映したHTML（`slide.html`）が出力できる | OK |
| 14 | 印刷ボタンが有効化され（`hasSlides()`が通り）、印刷用ウィンドウを開く処理へ進める | OK |
| 15 | 既存のMarkdownが入っている状態でサンプルボタンを押すと、`window.confirm()`の確認が表示される（文書/スライドそれぞれ専用の文言） | OK |
| 16 | 確認ダイアログでキャンセルすると、エディタの本文が変わらない | OK |
| 17 | キャンセルすると表示モード（文書/スライド）も変わらない | OK |
| 18 | キャンセルするとヘルプも開いたままになる | OK |
| 19 | 確認ダイアログでOKすると、エディタがサンプルへ置き換わる | OK |
| 20 | スライドサンプル挿入直後、Marp描画が完了する前は`isRenderCurrent()`が偽になり、HTML出力/印刷/プレゼン表示が「反映されていません」エラーで止まる（`hasSlides()`のガードが機能） | OK |
| 21 | 描画完了後は同ガードが通り、出力系操作が行える | OK |
| 22 | 文書サンプル→スライドサンプル→文書サンプル…と連続して挿入・モード切替しても、プレビュー・スライド枚数・保存対象のMarkdownが常に最新の内容と一致する | OK |
| 23 | 「使い方」「Markdown」「Marp」「プレゼン」の4タブ構成が維持されている | OK |
| 24 | サンプルボタン追加後も、Tabを40回押してもフォーカスがモーダル外へ抜けない | OK |
| 25 | Markdown早見表10件・Marp早見表9件の項目数が変わっていない（回帰） | OK |
| 26 | 早見表のコピーボタンが引き続き機能する（回帰） | OK |
| 27 | `Esc`でヘルプが閉じる（回帰） | OK |
| 28 | 背景（オーバーレイ）クリックで閉じる（回帰） | OK |
| 29 | サンプル挿入・プレビュー更新・モード切替・HTML出力・プレゼン表示を通じて外部への通信が発生しない（ネットワークログで確認） | OK |
| 30 | 上記操作を通じてconsoleエラーが発生しない | OK |

あわせて、AnyDoc変換、Markdownファイル読込、Markdown編集、文書/スライドプレビュー、
文書/スライド切替、Markdown保存、Markdownコピー、テーマ変更、印刷、発表者ビュー、
`isRenderCurrent()`による状態整合性、Marp Core遅延ロード、ヘルプの4タブ・早見表・
コピー・「コピー済み」表示・フォーカストラップ・Escape・背景クリックなど、
v0.1.0〜v0.4.3の主要機能一式を再実行し、回帰がないことを確認した。外部通信0件、
consoleエラー0件。

### v0.5.0まとめ

「使い方」タブに「サンプルで試す」を追加し、初めてMarkdownUtilを開いた利用者が、
実際のMarkdownを編集しながら「文書」「スライド」機能を理解できるようにした。
サンプル本文は`js/samples.js`に分離し、`help.js`はどちらが選ばれたかを通知するだけ、
実際の反映（エディタ更新・モード切替・確認ダイアログ）は`app.js`側が既存の
`handleFile()`/`clearAll()`と同じ更新経路（`editor.value`代入 → `applyModeUI()` →
`refreshActivePreview()`）を再利用する形で実装した。上部ツールバーへの新規ボタン追加、
カード型の大きなサンプル選択UI、複数種類のテンプレート、サンプルの自動保存・永続化は
行っていない。

### 既知の制限（v0.5.0で追加）

- 文書サンプル・スライドサンプルはそれぞれ1種類のみで、業務別テンプレートや利用者
  独自サンプルの登録・保存には対応していない。
- サンプルはボタン押下時にその場で挿入するのみで、初回起動時の自動表示や強制的な
  チュートリアル誘導は行わない。
- サンプル挿入はエディタ全体の置き換えであり、カーソル位置への部分挿入や、既存内容との
  マージには対応していない。

## v0.5.1（変換中のサンプル挿入競合修正）

実施日: 2026-08-13
方法: Playwright経由のChromiumで確認。PR #14のレビューで、Office/PDF変換中
（`setConverting(true)`でopenBtn/editor/clearBtn/slideThemeSelectを無効化している間）に
「? ヘルプ」からサンプルを挿入すると、変換完了時の`editor.value`上書き（`handleFile()`）と
競合し、挿入直後のサンプルが変換結果で突然消えてしまう不具合が指摘された。ヘルプ自体は
`helpBtn`が`setConverting()`の無効化対象に含まれておらず変換中も開けるため、
「開ける操作」と「Markdownを変更してよい操作」の非対称に対する既存ガードが
`insertSample()`に欠けていたことが原因だった。

### 修正内容

- `js/app.js`の`insertSample()`冒頭に、`state.isConverting`が真の間は挿入せず
  `setStatus('変換中はサンプルを開けません。変換完了後にもう一度お試しください。', 'error')`
  で案内して`return`するガードを追加した。ヘルプ自体は引き続き開閉・タブ切替が可能で、
  この変更は挿入操作（エディタ変更・モード切替・ヘルプを閉じる）だけを止める。
- ネットワークをフックしてAnyDoc WASMの読み込みを意図的に停止させ、`state.isConverting`が
  真のまま保持される状況を再現したうえで動作を確認した（実ファイルでの自然な変換完了待ちは
  タイミング依存で再現しづらいため）。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 変換中（`editor`が無効化されている間）に「文書サンプルを開く」を押しても、エディタの内容が変わらない | OK |
| 2 | 同様にスライドサンプルでもエディタの内容が変わらない | OK |
| 3 | 変換中にサンプルボタンを押しても、表示モード（文書/スライド）が変わらない | OK |
| 4 | 変換中にサンプルボタンを押しても、ヘルプが閉じない（案内メッセージのみ表示） | OK |
| 5 | 変換完了後（`editor`が再度有効化された後）は、通常どおりサンプルを挿入できる | OK |
| 6 | 変換中でなければ、従来どおり上書き確認・自動モード切替・ヘルプを閉じる・フォーカス復帰が機能する（回帰） | OK |

あわせて、v0.5.0で実施した30項目の回帰テストを再実行し、影響がないことを確認した。
外部通信0件、consoleエラー0件。

### v0.5.1まとめ

`insertSample()`に`state.isConverting`のガードを1箇所追加するだけの小さな修正で、
変換完了時の`editor.value`上書きとサンプル挿入が競合する問題を解消した。
`setConverting()`が管理する既存の無効化対象（openBtn/editor/clearBtn/
slideThemeSelect）自体は変更しておらず、ヘルプの開閉・タブ切替は引き続き変換中も
可能なままにしている。

### 既知の制限（v0.5.1で追加）

- なし（v0.5.0の既知の制限がそのまま引き続き適用される）。

## v0.6.0（Markdown → Word（DOCX）出力）

実施日: 2026-08-14
方法: Node.js上で`js/word-export.js`を直接呼び出しての単体検証（生成したDOCXを
`python-docx`という独立実装のPythonライブラリで読み戻し、段落スタイル・ランの
太字/斜体/フォント・表・リスト階層・numbering.xmlの内容を確認）と、Playwright経由の
Chromiumによる画面操作の両方で確認した。この環境では`soffice`（LibreOffice）の
headless変換が`Error: source file could not be loaded`で動作しなかった（自明な
`.txt→.docx`変換ですら失敗したため、生成物ではなく環境側の制約と判断）ため、
Microsoft Word互換性は「独立実装のリーダーで構造どおりに読み戻せること」と
「`docx`ライブラリ自体がMicrosoft/LibreOffice双方で広く実運用されている、ブラウザ/Node両対応を
謳う成熟したOOXML生成ライブラリであること」を根拠に確認した（実機Wordでの開封確認は
別途利用者側での実施を推奨）。

### 採用ライブラリ

- [`docx`](https://docx.js.org)（npm: `docx`、v9.7.1、MIT License）を採用した。
  ブラウザ対応・Node両対応を公式に謳っており、実際にChromium上で`Packer.toBlob()`が
  動作すること、`eval`/`require`の実行時呼び出しを含まないことを確認済み。
- npmパッケージの`dist/index.mjs`（Node/ブラウザ両対応のESMビルド、外部importなしの
  単一ファイル）を`esbuild`で再バンドル・minifyし、`vendor/docx/docx.esm.min.mjs`として
  配置した（1.06MB → 412KB）。CDNは使用していない。
- `docx`が内部でバンドルする依存（`jszip`＝MIT/GPL-3.0-or-laterのデュアルライセンスから
  MITを選択、`nanoid`・`hash.js`・`xml`・`xml-js`＝いずれもMIT）のライセンス全文を
  `LICENSES/docx-dependencies/`に保存し、`LICENSES/THIRD_PARTY_NOTICES.md`へ追記した。

### markdown-itトークン→Word要素の対応

HTMLを経由せず、`md.parse(markdown, {})`が返すブロック/インライントークンを
`js/word-export.js`内で直接読み進めてWord要素（`Paragraph`/`Table`/`TextRun`等）へ
変換している。

| Markdownトークン | Word要素 |
| --- | --- |
| `heading_open`（h1〜h6） | `Paragraph({ heading: HeadingLevel.HEADING_1〜6 })`（Word側の見出しスタイルとして認識される） |
| `paragraph_open` | `Paragraph`（リスト内なら`bullet`/`numbering`、引用内なら左罫線+インデントを付与） |
| `strong_open`/`em_open`/`code_inline` | `TextRun`の`bold`/`italics`/`font`（同一段落内で入れ子カウンタにより混在に対応） |
| `bullet_list_open`/`ordered_list_open`（入れ子含む） | `bullet: { level }` / 独自`numbering`参照（最大3階層） |
| `table_open`〜`table_close` | `Table`/`TableRow`/`TableCell`（ヘッダー行は太字+網掛け、罫線は既定で付与） |
| `fence`/`code_block` | 等幅フォント（Consolas）+ 網掛け+罫線の1段落（複数行は`TextRun`の`break`で改行を維持） |
| `hr` | 下罫線のみを持つ空段落（水平線） |
| `blockquote_open`〜`close` | 左罫線+インデントを付与した段落（可能なら対応、の範囲で実装） |
| `image`（対象外） | `[画像: 代替テキスト（Word出力は非対応）]`という斜体プレースホルダ |
| `link_open`/`link_close`（対象外） | リンク文字列のみプレーンテキストとして出力（ハイパーリンク化なし） |
| Marp front matter・HTMLコメント | `md.parse()`に渡す前に正規表現で除外（後述） |

見出しはビルトインの`HeadingLevel`定数を使ってpStyle参照にしているため、
Wordのナビゲーションウィンドウ・アウトライン表示・目次機能から見出しとして認識できる。

### md2docとの比較

同一リポジトリオーナーの参考実装 [`md2doc`](https://github.com/YanTKYS/md2doc)
（`src/Md2Doc.Core/OpenXmlConverter.cs`、Markdig + DocumentFormat.OpenXml、C#/WinForms）を
読み、変換方式・対応範囲を比較した（この環境に.NET SDKがなくビルド・実行はできないため、
実際の変換結果はコードレベルでの比較にとどめている）。

| 項目 | md2doc | MarkdownUtil（本実装） |
| --- | --- | --- |
| Markdown解析 | Markdig（`UseAdvancedExtensions`） | markdown-it（既存プレビューと共有、`markdown-engine.js`） |
| Word生成 | DocumentFormat.OpenXml SDK（.NET、ローカルアプリ） | `docx`（ブラウザ内で完結） |
| 見出し | H1〜H3のみスタイル定義（H4以上はH3スタイルへ丸める）。見出し番号の自動付与オプションあり | H1〜H6すべてに対応（見出し番号の自動付与機能はなし） |
| リストの最大階層 | 3階層（`MaxListLevel = 2`） | 3階層（`MAX_LIST_LEVEL = 2`）— 独立した設計判断で一致 |
| 番号付きリストの再開 | リストブロックごとに新しい`NumberingInstance`を生成し、常に1から始まる | 同様に、離れた場所にある別々の番号付きリストごとに新しいnumbering参照を割り当て、1から数え直すよう実装（v0.6.0で対応、テスト#18参照） |
| 表の列幅 | 最大セル数に合わせて均等割り（twips指定） | 表全体をwidth 100%として自動割り付け（列ごとの明示的な幅指定はなし） |
| コードブロック | 行ごとに独立した段落（網掛け・罫線なし） | 1つの段落内で改行を維持し、網掛け+罫線を付与（視覚的にコードブロックと分かりやすい） |
| 改ページ（`<!-- pagebreak -->`） | 対応 | 非対応（v0.6.0のスコープ外） |
| ヘッダー/フッター・ページ番号・見出し番号設定 | 対応（UI設定あり） | 非対応（v0.6.0でやらないことに明記） |
| 画像 | 未確認（コード上は非対応） | 明示的に非対応（代替テキストのプレースホルダを出力） |

文書構造・見出し・太字/斜体・リスト・表・コードのいずれについても、両実装とも
「同じ種類のMarkdown要素を、対応するWordの構造要素（見出しスタイル・
番号付き段落・表・等幅フォント）へ変換する」という基本方針は一致しており、
大きな欠落は確認されなかった。主な差異は上記の見出し対応レベル・コードブロックの
装飾・ヘッダー/フッター等の周辺機能であり、いずれもv0.6.0の対応範囲外として
README/`docs/TESTING.md`に明記した。

### Marp Markdownの扱い

- 先頭が`---`で始まり、閉じの`---`までの間がすべて`key: value`形式の行である場合に限り
  front matter とみなしてWord出力前に除去する（本文が単なる水平線`---`から始まる
  文書を誤って除去しないよう、キー:値の並びだけで構成される場合に限定）。
- HTMLコメント（`<!-- -->`）はスピーカーノート・通常のMarkdownコメントの両方を対象に、
  正規表現で除去してから`md.parse()`へ渡す。
- スライド区切りの`---`はfront matter除去後もそのまま残るため、markdown-itの通常解釈
  どおり水平線（`hr`）としてWord側に出力される。Marpのスライド構成を再現する機能は
  持たない。

### エラー処理

- 空・空白のみのMarkdownで「Word出力」を押した場合は、DOCXを生成せず
  「Word出力するMarkdownがありません。」を表示して終了する。
- `md.parse()`や`Document`/`Packer.toBlob()`が例外を投げた場合は`WordExportError`へ
  正規化し、「Markdownを解析できませんでした。」または
  「Wordファイルを作成できませんでした。」をステータス表示する。いずれの場合も
  ダウンロードは発生させない（`downloadBlob()`の呼び出し自体に到達しない）。
- Office/PDF変換中（`state.isConverting`）はWord出力ボタンを無効化し、押しても
  何も起きない。多重クリック防止用に`state.isExportingWord`も設け、生成中は
  ボタンを無効化する（3回連続クリックしてもダウンロードは1回のみ発生することを確認）。

### 外部通信確認

Playwrightでリクエストを監視し、Word出力（空Markdownでの試行、通常のMarkdown、
文書/スライドサンプル、複数回の番号付きリストを含む文書、日本語+絵文字を含む
長文500段落の文書）を含む一連の操作で、配信元オリジン（`http://localhost:.../`）
以外へのリクエストが0件であることを確認した。consoleエラーは、この環境に存在しない
`favicon.ico`への404（既存・無関係）以外は発生しなかった。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | Markdownが空の場合、「Word出力するMarkdownがありません。」と表示され出力しない | OK |
| 2 | Markdownがある場合に`.docx`ファイルとして保存できる | OK |
| 3 | 生成した`.docx`を独立実装（python-docx）で読み戻せる（段落・スタイル・表が破損なく取得できる） | OK |
| 4 | ファイル名が`document.md → document.docx`、`meeting-note.md → meeting-note.docx`等へ変換される | OK |
| 5 | Word出力に関わる操作で外部通信が発生しない | OK |
| 6 | H1がWordの見出し1スタイルとして出力される | OK |
| 7 | H2がWordの見出し2スタイルとして出力される | OK |
| 8 | H3がWordの見出し3スタイルとして出力される | OK |
| 9 | 通常段落が出力される | OK |
| 10 | 太字が`bold`ランとして出力される | OK |
| 11 | 斜体が`italic`ランとして出力される | OK |
| 12 | インラインコードが等幅フォント（Consolas）ランとして出力される | OK |
| 13 | 箇条書きが`bullet`（List Paragraphスタイル）として出力される | OK |
| 14 | 番号付きリストが独自numbering参照で出力される | OK |
| 15 | 表がヘッダー行（太字+網掛け）・データ行・罫線付きで出力される | OK |
| 16 | コードブロックが複数行を維持した等幅フォント+網掛け+罫線の段落として出力される | OK |
| 17 | 水平線が下罫線のみの空段落として出力される | OK |
| 18 | 離れた場所にある2つの番号付きリストが、それぞれ1から数え直される（md2doc比較を踏まえた対応） | OK |
| 19 | 1つの段落内で通常文字・太字・斜体・インラインコードが混在し、それぞれ独立したランとして出力される | OK |
| 20 | 日本語・絵文字・異体字を含む500段落の長文がすべて欠落・文字化けなく出力される | OK |
| 21 | 文書サンプル（見出し・太字・斜体・番号付きリスト・箇条書き・表・引用を含む）をそのままWord出力できる | OK |
| 22 | Marp front matter（`marp:`/`theme:`/`paginate:`）がWord本文に出力されない | OK |
| 23 | スピーカーノート用HTMLコメントがWord本文に出力されない | OK |
| 24 | スライドMarkdown（front matter・スピーカーノート・複数スライド`---`区切りを含む）をWord出力してもエラーにならず、スライド区切りは水平線として出力される | OK |
| 25 | Office/PDF変換中はWord出力ボタンが無効化され、クリックしても何も起きない | OK |
| 26 | Word出力ボタンを3回連続クリックしても、ダウンロードは1回のみ発生する | OK |
| 27 | `md.parse()`や`Packer`が失敗するケースを`WordExportError`で捕捉し、ステータス表示のみで壊れたファイルをダウンロードさせない（コード上のtry/catchで確認。人為的に発生させる自然な失敗ケースは v0.6.0の対応範囲内では未発見） | OK |
| 28 | Office/PDF → Markdown変換（回帰） | OK |
| 29 | 文書プレビュー（回帰、`preview.js`の`markdown-engine.js`共有化後も出力内容が変化しないことを確認） | OK |
| 30 | スライドプレビュー（回帰） | OK |
| 31 | Markdown保存（回帰） | OK |
| 32 | Markdownコピー（回帰） | OK |
| 33 | HTML出力（回帰） | OK |
| 34 | 印刷（別ウィンドウが`blob:`URLで開くことを確認、回帰） | OK |
| 35 | プレゼン表示・発表者ビュー（回帰） | OK |
| 36 | サンプル挿入（上書き確認・キャンセル時に本文/モード/ヘルプが変わらないことを含めて回帰） | OK |
| 37 | ヘルプ（4タブ構成・フォーカストラップ・Escで閉じる、回帰） | OK |
| 38 | 外部通信0件（Word出力を含む一連の操作全体で確認） | OK |
| 39 | consoleエラー0件（favicon 404を除く） | OK |

### v0.6.0まとめ

現在のMarkdownを、サーバやMicrosoft Wordを使わずブラウザ内だけでMicrosoft Word形式
（.docx）へ変換・保存する機能を追加した。既存のMarkdown解析（`markdown-it`）を
`markdown-engine.js`として共通化し、HTMLを経由せずトークンを直接Word要素へ変換する
方式（`js/word-export.js`）で実装した。参考実装`md2doc`のC#実装を読み、見出し・
リスト・表・コード等の対応方針を比較したうえで、番号付きリストの再開挙動などの
差異は本実装側でも同等の挙動になるよう調整した。Marp向けのfront matter・
HTMLコメントはWord本文から除外し、画像埋め込み・ハイパーリンク化・テンプレート
選択・詳細書式設定等はv0.6.0のスコープ外として明記した。

### 既知の制限（v0.6.0で追加）

- Markdown画像（`![alt](src)`）はWordへ埋め込まれず、代替テキストのプレースホルダに
  置き換わる。
- リンク（`[text](url)`）はハイパーリンク化せず、プレーンテキストとして出力される。
- Marp向けMarkdownをWord出力しても、スライド構成（1枚ずつの区切り・テーマ・
  背景色等）を再現するものではない。スライド区切りの`---`は単なる水平線として
  出力される。
- フォント選択・見出し番号の自動付与・ヘッダー/フッター・ページ番号・余白・
  用紙サイズ・縦横切替・脚注・目次自動生成の設定画面はない。既定のスタイル
  （本文: Yu Gothic 10.5pt、コード: Consolas）で出力する。
- 生成した`.docx`をMarkdownUtilへ再度取り込む機能（Word → Markdown変換）はない。
- この開発環境には`soffice`（LibreOffice）が動作する状態で存在せず、また
  Microsoft Word・.NET SDKも利用できないため、実機のWord/LibreOffice双方での
  目視確認は行えていない。生成した`.docx`の妥当性は、独立実装のPythonライブラリ
  （python-docx）による読み戻し確認と、`docx`ライブラリの実績・仕様準拠を根拠に
  判断している。実機での最終確認は別途利用者側での実施を推奨する。

## v0.6.1（Word出力中のファイル切替競合修正）

実施日: 2026-08-14
方法: Playwright経由のChromiumで確認。PR #16のレビューで、`exportWord()`が
`editor.value`（Markdown本文）は`buildDocxBlob()`呼び出し時点の値を使う一方、
保存ファイル名は`await`完了後に`state.saveFilename`から取得していた点を指摘された。
Word生成中に無効化されるのは「Word出力」ボタンのみで、「ファイルを開く」
（`openBtn`）はOffice/PDF変換中（`state.isConverting`）以外では無効化されない。
そのため、長文で生成に時間がかかる間に別のMarkdown/文書ファイルを開くと、
`state.saveFilename`が新しいファイル名に変わり、「Aの内容を持つDOCXが
Bのファイル名で保存される」という内容とファイル名の不一致が発生し得た。

### 修正内容

- `js/app.js`の`exportWord()`冒頭で、Markdown本文（`editor.value`）と出力
  ファイル名（`docxFilenameFromSaveFilename()`の結果）の両方を、Word出力を
  押した時点の値としてローカル変数へスナップショットするよう変更した。
  `buildDocxBlob()`への`await`完了後もこのスナップショットをそのまま使うため、
  生成中に別ファイルを開いて`editor.value`/`state.saveFilename`が変わっても、
  「押した瞬間の文書」が「その文書のファイル名」で保存される。
- `openBtn`自体の無効化条件（Office/PDF変換中のみ）は変更していない。ヘルプの
  「使い方」タブ相当の考え方（変換中は変更操作のみ止める）を踏襲しつつ、
  Word出力については非同期処理の入力値をスナップショットする形で解決した。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 長文MarkdownでWord出力を開始し、生成中（ボタン無効化中）に別のMarkdownファイルを開いても、保存されるDOCXは出力開始時点の文書の内容になる | OK |
| 2 | 上記の場合、保存されるファイル名も出力開始時点のファイル（例: A.md → A.docx）のものになる | OK |
| 3 | 通常のWord出力（生成中に何も操作しない場合）の内容・ファイル名は従来どおり | OK |
| 4 | 文書 / スライドサンプルのWord出力が引き続き正しく動作する（回帰） | OK |
| 5 | 空Markdownでの出力抑止、変換中の無効化、多重クリック防止、外部通信0件、consoleエラー0件（回帰） | OK |

### v0.6.1まとめ

`exportWord()`内でMarkdown本文とファイル名を出力開始時点にスナップショットする
1箇所の修正で、Word生成中の非同期区間に別ファイルを開いた場合の内容・ファイル名の
不一致を解消した。`state.isExportingWord`によるボタン無効化・多重クリック防止など、
v0.6.0で実装した他の状態管理は変更していない。

### 既知の制限（v0.6.1で追加）

- なし（v0.6.0の既知の制限がそのまま引き続き適用される）。
- レビューで指摘された、Word出力ライブラリ（`docx`）が`app.js`から静的import
  されているためWord出力を使わない場合も初期ロード対象になる点は、v0.6.1では
  対応していない（Marp Coreほどのサイズではないため優先度は低いが、起動速度が
  問題になった場合はMarp同様の動的importを検討する）。
- Microsoft Word実機での文書サンプル出力の目視確認（見出し・表・日本語フォント・
  番号付きリストの表示）は、この開発環境では実施できていない（v0.6.0の既知の制限を
  参照）。

## v0.6.2（Word出力の表示崩れ修正とリファクタリング）

実施日: 2026-08-14
方法: Node.js上で`js/word-export.js`を直接呼び出し、生成したDOCXの`word/document.xml`を
読み出して`w:gridCol`・`w:numPr`・`w:ind`を確認。あわせてPlaywright経由のChromiumで
既存機能一式の回帰を確認した。ソース全体を読み直し、職員が通常の業務範囲で使う際に
支障となる不具合を優先して修正し、あわせて重複・複雑さを整理した。

### 修正した不具合

**1. 表の列幅が極端に狭くなる（表を含むすべての文書に影響）**

`buildTable()`が`width: 100%`だけを指定していたため、`docx`ライブラリが
`w:tblGrid`へ列幅として`100`（twips。約1.8mm）をそのまま書き出していた。
Word側の自動調整に頼る形になり、閲覧環境によっては表が本文幅いっぱいに
広がらず、極端に狭い列で描画される可能性があった。
行ごとのセル数から列数を求め、本文幅（A4縦の幅11906から左右余白1440×2を引いた
9026twips）を等分した幅を`columnWidths`として明示するようにした。
参考実装`md2doc`も同様に`w:tblGrid`へ実寸を書き出しており、方針を合わせている。
修正後は3列の表で`<w:gridCol w:w="3008"/>`（3008×3 ≒ 9026）が出力される。

**2. リスト項目の2段落目が別の項目として出力される（手順書・マニュアル類に影響）**

1つのリスト項目に複数の段落がある場合（項目のあとに補足説明を続けた場合）、
2段落目以降にも中黒や番号が付き、別の項目のように見えていた。
例えば「申請書を提出する／（補足）提出先は総務課です。／受理通知を待つ」が
3項目の手順として出力され、手順の数を読み違えるおそれがあった。
`list_item_open`で「次の段落が項目の1段落目」という印を付け、その段落にだけ
中黒/番号を付けるようにした。2段落目以降は記号を付けず、項目本文と同じ位置まで
字下げして続きであることを示す。参考実装`md2doc`も同じ方針を採っている。

### リファクタリング

- **クリップボード処理の重複を解消**: `app.js`と`help.js`に1文字も違わない
  `legacyCopy()`（一時textarea + `execCommand`による代替コピー）が重複していた。
  `js/clipboard.js`へ`copyText()`として切り出し、成否を`boolean`で返す形に統一した。
  呼び出し側は`try/catch`とAPI分岐が不要になり、両ファイルで約15行ずつ削減した。
  あわせて、`copySnippet()`内の「呼び出し元が常にボタンを渡すため到達しない」
  `if (triggerBtn)`の判定（実質的な死んだコード）も除去した。
- **ファイル名生成の重複を解消**: `titleFromSaveFilename()`と
  `docxFilenameFromSaveFilename()`が同じ正規表現で拡張子を落とす処理を
  別々に持っていた。拡張子を除いた部分を返す`saveFilenameBase()`ひとつに統一し、
  Word出力側は`` `${saveFilenameBase()}.docx` ``と組み立てる形にした。
- **`insertSample()`の引数を整理**: 位置引数5個
  （`sample, mode, filename, confirmLabel, successMessage`）は呼び出し側から
  意味が読み取りにくかった。サンプルの定義を`SAMPLES`テーブル
  （キーがそのまま表示モード）へまとめ、`insertSample('doc')` /
  `insertSample('slide')`と書けるようにした。確認文言と完了メッセージは
  `label`から組み立てるため、同じ語（「文書サンプル」等）の重複記述もなくなった。
- **番号付きリストのレベル定義を整理**: ほぼ同一のオブジェクト3個の直書きを、
  字下げ幅を返す`listIndentLeft()`とループで生成する形にした。この字下げ幅は
  上記の不具合2の「2段落目以降の字下げ」でも再利用しており、リストの記号側と
  本文側の位置が必ずそろう。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 3列の表で`w:gridCol`が3008twips（本文幅の3等分）になる | OK |
| 2 | 表の内容・ヘッダー行の太字/網掛け・罫線が従来どおり出力される | OK |
| 3 | 箇条書き項目の2段落目に中黒が付かず、字下げのみになる | OK |
| 4 | 番号付きリスト項目の2段落目に番号が付かず、字下げのみになる | OK |
| 5 | リスト項目の1段落目には従来どおり中黒/番号が付く | OK |
| 6 | 入れ子リスト（3階層）の階層が従来どおり出力される | OK |
| 7 | 離れた2つの番号付きリストがそれぞれ1から数え直される（v0.6.0の挙動を維持） | OK |
| 8 | Markdownコピーが動作し、クリップボードへ実際に反映される | OK |
| 9 | ヘルプの記法例コピーが動作し、「コピー済み」表示に切り替わる | OK |
| 10 | 文書サンプル挿入・自動モード切替・上書き確認が従来どおり動作する | OK |
| 11 | 文書プレビュー（見出し・表・水平線）が従来どおり描画される | OK |
| 12 | スライドプレビュー・テーマ変更・HTML出力・印刷・プレゼン表示・発表者ビューが従来どおり動作する | OK |
| 13 | Markdown保存・Word出力のファイル名が従来どおり（`document.md` / `document.docx`） | OK |
| 14 | ヘルプ4タブ・早見表10件/9件・フォーカストラップ・Escで閉じるが従来どおり動作する | OK |
| 15 | 外部通信0件 | OK |
| 16 | consoleエラー0件（`favicon.ico`の404を除く） | OK |

### v0.6.2まとめ

Word出力で「表が狭く描画される」「手順書のリストが項目数を誤って見せる」という、
通常の業務文書でそのまま影響が出る2件を修正した。あわせて、完全に重複していた
クリップボード処理を`js/clipboard.js`へ切り出し、ファイル名生成の重複・
位置引数5個の関数・直書きのレベル定義を整理した。機能の追加・変更はなく、
既存の動作は回帰テストで維持を確認している。

### 既知の制限（v0.6.2で追加）

- コードブロックの中にHTMLコメント（`<!-- -->`）を書いた場合、Word出力では
  そのコメント行が取り除かれる。Word出力前のfront matter/コメント除去を
  Markdown全体への正規表現で行っているため。コード例としてHTMLコメントを
  掲載する文書は通常の利用では想定しにくく、回避のために除去処理を
  複雑にすることは避けた。
- v0.6.1までの既知の制限（画像の非埋め込み、リンクの非ハイパーリンク化、
  詳細書式設定画面なし、Word実機での目視確認が未実施 など）はそのまま適用される。

## v0.6.3（エラー処理の堅牢化）

実施日: 2026-08-14
方法: `python3 -m http.server`でリポジトリ本体を配信（http://127.0.0.1:8801 ）し、
GUI Chromium上で画面操作して確認した。失敗経路は、リポジトリを`/tmp`へコピーした
配信物（http://127.0.0.1:8810 ）に対してのみ障害注入を行い、URLの`?fail=<種別>`で
切り替えた（本体のコードは変更していない）。注入内容は
`window.open`をnullにする／開いたウィンドウの`document.write`を例外にする／
`navigator.clipboard.writeText`を拒否し`execCommand`をfalseにする／
スライド枠への`postMessage`を例外にする／`downloadTextFile()`を例外にする／
`buildStandaloneHtml()`をnullにする／`slidePreview.render()`を例外にする／
`markdown-it`の`parse`・`render`を例外にする／`help.init()`を例外にする／
vendorの`Marp#render`を例外にする、の各1点差し替え。
AnyDocの変換失敗は、注入ではなく不正なZIP構造の`.docx`（41バイト）を
本体（8801）で実際に読み込んで再現した。
外部通信とconsoleは、Playwright（CDP接続）でリクエスト・console・pageerrorを
記録しながら正常系一式を実行して集計した。

### 実施したテスト

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | `buildStandaloneHtml()`がnullを返すとき、`スライドをHTMLとして出力できませんでした。スライドの表示を確認してください。`をエラー表示し、`保存しました`を出さない | OK |
| 2 | 同条件でHTMLファイルがダウンロードされない（中身が`null`のファイルが増えない） | OK |
| 3 | `downloadTextFile()`が例外を投げるとき、Markdown保存は`ファイルを保存できませんでした。`をエラー表示し、`保存しました`を出さない | OK |
| 4 | 同条件でHTML出力は`HTMLファイルを保存できませんでした。`をエラー表示し、`保存しました`を出さない | OK |
| 5 | 上記3・4でファイルがダウンロードされず、consoleに`[MarkdownUtil] saveMarkdown: document.md の保存に失敗`・`[MarkdownUtil] slideExport: document.html の保存に失敗`が各1件出る | OK |
| 6 | `window.open()`がnullのとき、プレゼン表示は`プレゼン用ウィンドウを開けませんでした。ポップアップの許可を確認してください。`を表示し、発表者ビューへ切り替わらない | OK |
| 7 | 同条件で印刷は`印刷用ウィンドウを開けませんでした。ポップアップの許可を確認してください。`を表示する | OK |
| 8 | 上記6・7の後もテーマ変更・スライド再描画が動作する（本体が壊れない） | OK |
| 9 | ポップアップへの`document.write()`が失敗したとき、`プレゼン用ウィンドウを開けませんでした。…`を表示し、consoleに`[MarkdownUtil] start: プレゼン用ウィンドウの初期化に失敗`が出る | OK |
| 10 | 同条件で空のウィンドウが残らない（操作後もブラウザのページ数が押す前と同じ1件） | OK |
| 11 | 同条件のまま続けてHTML出力が正常に完了する（`document.html を保存しました`） | OK |
| 12 | `slidePreview.render()`がrejectしたとき、`スライドを準備しています...`で止まらず`スライドを表示できませんでした。`を表示し、consoleに`[MarkdownUtil] refreshSlidePreview: スライドプレビューの描画に失敗`が出る | OK |
| 13 | `copyText()`が失敗したとき、画面は従来どおり`コピーに失敗しました。テキストを選択して手動でコピーしてください。`を表示する | OK |
| 14 | 同条件でconsoleに`[MarkdownUtil] copyText: クリップボードへのコピーに失敗`が原因（Error本体）付きで出る | OK |
| 15 | 不正な構造の`.docx`を読み込むと`ファイルが破損しているか、読み取れない構造のため変換できませんでした。`を表示し、consoleに`[MarkdownUtil] convertToMarkdown: AnyDocでの変換に失敗`がAnyDocの原因（`Could not find EOCD`）付きで出る | OK |
| 16 | 同条件でconsoleに`[MarkdownUtil] handleFile: broken.docx の読み込み・変換に失敗 ConversionError`も出る | OK |
| 17 | Marpの解析が失敗したとき、スライド用ステータスに`Markdownを解析できませんでした。front matterの書式を確認してください。`を表示し、consoleに`[MarkdownUtil] render: MarpでのMarkdown解析に失敗`が出る | OK |
| 18 | 同条件でHTML出力を押してもファイルは出力されない（スライド0枚のため何も起きない） | OK |
| 19 | Markdown解析が失敗したとき、Word出力は`Markdownを解析できませんでした。`を表示し、consoleに`[MarkdownUtil] buildDocxBlob: Markdownの解析・Word要素への変換に失敗`と`[MarkdownUtil] exportWord: document.docx の生成・保存に失敗 WordExportError`が出る（`WordExportError`と元の例外の両方がログに残る） | OK |
| 20 | 文書プレビューの描画が失敗したとき、`プレビューを更新できませんでした。表示は直前の内容のままです。`を表示し、consoleに`[MarkdownUtil] refreshDocPreview: 文書プレビューの描画に失敗`が出る | OK |
| 21 | `init()`が失敗したとき、`画面の初期化に失敗しました。ページを再読み込みしてください。`を表示し、consoleに`[MarkdownUtil] init: 起動処理に失敗`が出る | OK |
| 22 | スライド枠への`postMessage`が失敗しても、consoleに`[MarkdownUtil] createMessagePort: スライド描画先へのpostMessageに失敗`が出るだけで、スライド枚数表示（`2枚`）とプレゼン表示（発表者ビューの現在/次スライド描画）は動作する | OK |
| 23 | 回帰: 文書サンプル挿入→文書プレビュー（見出し・箇条書き・表・引用）が描画される | OK |
| 24 | 回帰: Word出力で`document.docx`（9,936バイト）が保存される | OK |
| 25 | 回帰: 出力した`document.docx`を読み込み直すと`document.docx → Markdown変換完了`となり、エディタにMarkdownが入る（AnyDoc変換） | OK |
| 26 | 回帰: スライドサンプル挿入→スライドタブでスライドが描画され`5枚`と表示される | OK |
| 27 | 回帰: テーマを`gaia`へ変更するとfront matterが`theme: gaia`になり、表示も切り替わる | OK |
| 28 | 回帰: HTML出力で`slide.html`（33,356バイト）が保存され、`slide.html を保存しました`と表示される | OK |
| 29 | 回帰: 印刷で別タブが開き、5ページ分のスライドが印刷プレビューに表示される | OK |
| 30 | 回帰: プレゼン表示で投影用ウィンドウが開き、本体が発表者ビュー（現在/次/スピーカーノート/`1 / 5`）になる。`次へ →`で`2 / 5`になり、`終了`で通常画面へ戻る | OK |
| 31 | 回帰: Markdownをコピーで`Markdownをコピーしました`、Markdownを保存で`slide.md`（1,556バイト）が保存される | OK |
| 32 | 回帰: クリアの確認ダイアログをOKするとエディタが空になり、文書モードと初期メッセージへ戻る | OK |
| 33 | 正常系一式（サンプル挿入・Word出力・スライド・テーマ・HTML出力・コピー・保存）でconsoleのerror・warning・pageerrorが0件 | OK |
| 34 | 外部通信0件（記録した18リクエストはすべて配信元`127.0.0.1:8801`とblob URL） | OK |

### v0.6.3まとめ

エラー処理の堅牢化として、「失敗しているのに成功したように見える」経路と
「失敗すると画面が止まる／後片付けが残る」経路をUI操作で確認した。
とくに、HTML組み立てに失敗したときに中身が`null`のファイルを
「保存しました」として渡してしまう問題、`downloadTextFile()`の失敗が
成功表示になる問題、`document.write()`失敗時に空のポップアップが残る問題は、
障害注入で再現したうえで修正後の挙動（エラー表示・ファイル未作成・
ウィンドウ数が増えない）を確認している。
`logError()`は`[MarkdownUtil]`接頭辞付きでconsoleへ出力するだけで、
正常系ではerror・warningともに0件、外部通信も0件だった。

### 補足（この記録の範囲）

- 失敗経路は、実際のブラウザ制限（ポップアップブロック設定・クリップボード権限拒否など）
  ではなく、コピーした配信物への1点差し替えによる再現である。
- `ConversionError.cause`・`WordExportError.cause`は、原因の例外がconsoleへ
  出力されていることで確認した（プロパティ値そのものの検査は行っていない）。
- Marp解析失敗時にHTML出力を押した場合は、スライドが0枚のためボタン側の
  ガードで何も起きない（エラー文言も出ない）。ファイルが出力されないことは確認済み。

## v0.6.4（Devin並行PR成果の統合）

実施日: 2026-08-14
方法: v0.6.3までの正式な土台（PR #19マージ後のmain）へ、同じv0.6.2付近から独立して
作成された3件のPull Requestの成果を統合した。3件をそのままmerge/cherry-pickするのではなく、
それぞれの意図と実装を読み取ったうえでv0.6.3のエラー処理を維持する形に再構成し、
`node --test`によるユニットテストと、Playwright（Chromium、CDP接続）による実ブラウザでの
回帰・セキュリティ確認の両方を実施した。ブラウザ確認は`python3 -m http.server`で
リポジトリ本体を配信（http://127.0.0.1:8801 ）し、実際のダウンロード・別ウィンドウ（ポップアップ）
イベントを捕捉して検証した。

### 統合したPR

| PR | 内容 | 統合方針 |
| --- | --- | --- |
| [#20](https://github.com/YanTKYS/markdownutil/pull/20) | 共通処理の整理・重複削減（`js/dom.js` / `js/download.js` / `js/inline-html.js`の新設） | 採用。ただしv0.6.3の`logError()`によるエラー処理をこれらの共通モジュール側にも反映させたうえで、app.js/help.js/preview.js/presenter.js/slide-preview.jsを統合ブランチのv0.6.3コードへ合わせて再適用した |
| [#21](https://github.com/YanTKYS/markdownutil/pull/21) | postMessageの同一オリジン制限・CSP追加 | 採用。`index.html`へCSPを追加し、`slide-preview.js`の`buildFrameDocument()`（iframe/別ウィンドウ側の描画用ドキュメント）と`createMessagePort()`（本体側）の両方に送信元window・オリジンの検証を追加した |
| [#22](https://github.com/YanTKYS/markdownutil/pull/22) | Node標準機能（`node --test`）のみによるユニットテスト基盤 | 採用。既存11ファイルのテストをそのまま取り込み、v0.6.4で変更した箇所（`buildFrameDocument()`の`postMessage`呼び出し文字列）に合わせて2箇所のアサーションを更新。加えて新設した3モジュール（dom.js/download.js/inline-html.js）・`errors.js`・postMessageのorigin検証・v0.6.3のエラー経路（document.write失敗等）向けのテストを追加した |

### 競合と解消方法

3件は同じv0.6.2付近から派生しており、`js/app.js`・`js/help.js`・`js/presenter.js`・
`js/slide-preview.js`の変更が重なっていた（特にPR #20とPR #21は、いずれも
`slide-preview.js`の`buildFrameDocument()`/`createMessagePort()`を変更していた）。
単純なcherry-pick/mergeでは、PR #19（v0.6.3）のエラー処理を古い実装で上書きしてしまう
おそれがあったため、以下の優先順位で読み替えながら手動で再構成した。

1. 現在のmain（PR #19 / v0.6.3）のエラー処理・ガード条件をすべて維持する
2. PR #21のセキュリティ修正（origin検証・CSP）を、v0.6.3のtry/catch・`logError()`と
   両立する形で組み込む
3. PR #20の共通化（重複コードの切り出し）を、1・2で確定した実装に合わせて適用する
4. PR #22のテストコードを、1〜3で確定した最終的な実装へ追従させる

具体的な組み替え箇所:

- **`slide-preview.js`の`createMessagePort().send()`**: PR #19は`win.postMessage(message, '*')`を
  try/catchで囲み、失敗時は`logError()`のみで他の送信先への同期を止めない実装だった。PR #21は
  同じ関数を送信先オリジンの算出（`targetOrigin`）付きの実装へ変更していたが、try/catchを
  持たない版だった。統合では、PR #21の`targetOrigin`算出をtry/catchの**内側**（`win.postMessage(message, targetOrigin)`）に組み込み、両方の要件を満たした
- **`slide-preview.js`の`buildFrameDocument()`が返す描画用ドキュメント**: PR #20は
  `escapeHtml()`等をinline-html.jsへ切り出す一方、PR #21は同じ関数へ`host`/`hostOrigin`と
  送信元検証を追加していた。統合では、PR #20切り出し後のescapeHtml呼び出しを保ったまま、
  PR #21の`host`/`hostOrigin`変数とメッセージ受信時の検証（`event.source !== host || event.origin !== self.origin`）を追加した
- **`slide-preview.js`の`openPrintWindow()`**: PR #19はBlob URL生成失敗時の即時revoke・
  `logError()`記録を持つ実装、PR #20は同じ処理を`download.js`の`openTextInNewWindow()`へ
  切り出す一方でエラー処理を持たない実装だった。統合では、`download.js`側の
  `openTextInNewWindow()`にPR #19のtry/catch・即時revoke・`logError()`をすべて移し、
  `openPrintWindow()`はそれを呼ぶだけにした（挙動は変えていない）
- **`presenter.js`の`init()`/`syncAll()`**: PR #20の`initFrame()`/`showSlide()`への
  責務分割はそのまま採用し、PR #19が持つ`window.open()`・`document.write()`の
  try/catch・`logError()`・空ウィンドウの後始末（`closePopup()`）には触れていない
- **`app.js`/`help.js`**: PR #20の`downloadBlob`/`downloadTextFile`の切り出し、
  `setTabSelected()`・`buildCopyButton()`等のDOM生成の共通化を採用しつつ、
  `saveMarkdown()`/`exportWord()`のtry/catch＋`logError()`＋利用者向けメッセージ
  （PR #19）はそのまま呼び出し側に残した

### v0.6.3から維持した箇所（回帰なし）

以下はコード上そのまま維持し、後述のユニットテスト・ブラウザ確認の両方で
回帰していないことを確認した。

- `js/errors.js`・`logError(context, error)`（`[MarkdownUtil]`接頭辞付きでconsoleへ記録）
- 利用者向けメッセージと開発者向けconsole記録の分離
- HTML出力・Markdown保存の失敗時に成功扱いしない
- Word出力失敗時の原因記録（`WordExportError.cause`）
- 文書プレビュー例外処理・スライドプレビューの予期しないreject処理
- `window.open()`失敗処理・`document.write()`失敗時の後始末（空ウィンドウを残さない）
- 印刷用Blob URLの適切な解放
- `postMessage()`失敗時に他の描画先まで停止させない
- AnyDoc / Marp / Clipboard等の失敗原因記録
- `help.close()`の安全なガード・`init()`失敗時の利用者向け表示

### ユニットテスト結果

```
npm test
# tests 138
# suites 0
# pass 138
# fail 0
```

`npm run test:coverage`（`vendor/`・`test/`除外）:

| file | line % | branch % | funcs % |
| --- | --- | --- | --- |
| clipboard.js | 100.00 | 100.00 | 100.00 |
| converter.js | 92.42 | 74.19 | 100.00 |
| dom.js | 100.00 | 100.00 | 100.00 |
| download.js | 100.00 | 100.00 | 60.00 |
| errors.js | 100.00 | 100.00 | 100.00 |
| inline-html.js | 100.00 | 100.00 | 100.00 |
| markdown-engine.js | 100.00 | 100.00 | 100.00 |
| presenter.js | 98.84 | 89.71 | 100.00 |
| preview.js | 100.00 | 100.00 | 100.00 |
| samples.js | 100.00 | 100.00 | 100.00 |
| slide-preview.js | 97.34 | 93.67 | 92.00 |
| word-export.js | 98.14 | 91.86 | 100.00 |
| **all files** | **97.92** | **91.12** | **95.51** |

`js/app.js`・`js/help.js`はPR #22の方針を踏襲し、画面全体の組み立て（UI配線）が
中心のためユニットテストの対象外とし、本章のブラウザ確認で扱った。

新規に追加したテスト（PR #22の既存テストに加えて）:

- `test/dom.test.js` / `test/download.test.js` / `test/inline-html.test.js`:
  PR #20で切り出した共通モジュール3件。切り出し前にapp.js/help.js/slide-preview.js等へ
  直書きされていたのと同じ挙動（Blob URLの遅延/即時revoke、`aria-selected`の反映等）を
  維持していることを確認
- `test/errors.test.js`: `logError()`が`[MarkdownUtil]`接頭辞付きでcontextとErrorを
  そのままconsole.errorへ渡すことを確認
- `test/postmessage-security.test.js`: PR #21のorigin検証を`createMessagePort()`に対して
  直接検証。正しい送信元window・オリジンからのメッセージは受け付ける、送信元windowが
  異なるメッセージは拒否する、送信元windowは正しくてもオリジンが異なれば拒否する、
  送信時は`'*'`ではなく本体自身のオリジンを送信先に指定する（origin不明時のみ`'*'`）、
  origin検証を追加してもv0.6.3の「1つの送信先の失敗が他の送信先への同期を止めない」
  エラー処理が維持されることを確認
- `test/presenter.test.js`に追加: `document.write()`失敗時にfalseを返し空ウィンドウを
  残さないこと（v0.6.3のエラー処理）、origin検証が有効な状態でも正しいオリジンからの
  readyで発表者ビューが同期すること、異なるオリジンからのメッセージは同期に反映されないこと
- 既存の`test/slide-preview-render.test.js`・`test/presenter.test.js`のうち、
  `buildFrameDocument()`が返す`postMessage`呼び出しの文字列を直接検査していた
  2箇所は、PR #21のorigin検証を組み込んだ新しい実装（`host.postMessage(message, hostOrigin)`）
  に合わせてアサーションを更新した（動作の意味は変えていない）

### ブラウザ回帰結果

Playwright（Chromium、実ブラウザ）で、`http://127.0.0.1:8801` に配信した本体を
1セッション内で一通り自動操作して確認した（`download`イベント・別ウィンドウ
（`page`イベント）を実際に捕捉）。

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | 初期表示（エディタ・ステータス表示） | OK |
| 2 | 文書プレビュー（見出し・表・コード・強調） | OK |
| 3 | Markdownコピー（Clipboard API、成功メッセージ） | OK |
| 4 | Word出力（`document.docx`のダウンロード） | OK |
| 5 | AnyDoc変換（出力した`.docx`を読み込み直し、`→ Markdown変換完了`・見出しを含む） | OK |
| 6 | スライド表示（front matter付きMarkdownで「2枚」） | OK |
| 7 | スライドHTML出力（ファイルのダウンロード） | OK |
| 8 | 印刷（Blob URLの別タブが開く） | OK |
| 9 | プレゼン表示（投影用ウィンドウが開く） | OK |
| 10 | 発表者ビュー・スピーカーノート（「1枚目のノート」を表示） | OK |
| 11 | 発表者ビューの現在位置（`1 / 2`）・「次へ」で`2 / 2` | OK |
| 12 | プレゼン終了で投影用ウィンドウが閉じる | OK |
| 13 | ヘルプの開閉・タブ切替（Markdownタブ）・Escで閉じる | OK |
| 14 | 外部通信0件（`http://127.0.0.1:8801`以外へのリクエストなし） | OK |
| 15 | consoleエラー0件（`favicon.ico`の404を除く） | OK |

v0.1.0〜v0.6.3で個別に確認済みのテーマ変更・キーボード操作・focus trap・
背景クリック・多重クリック防止・Word生成中のファイル切替安全性等は、実装箇所を
変更していないため今回のPlaywright確認では再実行していない（コードの変更範囲は
dom.js/download.js/inline-html.jsへの切り出しとslide-preview.jsのorigin検証・
CSP追加のみで、これらの機能のロジック自体には触れていない）。

### セキュリティ確認

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | CSP: `connect-src 'self'`により、ページ内からの外部ホストへの`fetch()`が拒否される | OK（`Failed to fetch`で拒否） |
| 2 | CSP下でも文書プレビュー・スライド表示・Word出力・HTML出力・印刷・プレゼン表示が正常に動作する（上記ブラウザ回帰と同一セッションで確認） | OK |
| 3 | CSP下でもAnyDoc WASM（`wasm-unsafe-eval`）が動作する（AnyDoc変換の回帰テストと同一） | OK |
| 4 | CSP下でもMarp・スライド描画用iframeのインラインscript（`unsafe-inline`）が動作する | OK |
| 5 | Markdown中の`<script>`・`onerror`属性がプレビューで実行されない（`html: false`、CSP双方の効果） | OK |
| 6 | `createMessagePort()`は送信元window・オリジンが一致しないメッセージを拒否する（`test/postmessage-security.test.js`で単体確認） | OK |
| 7 | `createMessagePort()`の送信時オリジン指定は`'*'`ではなく本体自身のオリジンを使う（origin不明時のみ`'*'`にフォールバック） | OK |
| 8 | 外部通信0件（ブラウザ回帰と同一セッションで確認） | OK |

origin検証（#6・#7）はNode上のユニットテストで`createMessagePort()`を直接検証した。
実際に別オリジンのページから`window.open()`経由でMarkdownUtilの窓を取得し
`render`メッセージを送りつける攻撃の再現までは、テスト用の別オリジンサーバーを
別途用意する必要があり、今回のPlaywright確認には含めていない（「既知の制限」参照）。

### IIS / GitHub Pages観点

- CSPは`<meta http-equiv="Content-Security-Policy">`で`index.html`に直接記述しており、
  IIS側の追加設定（レスポンスヘッダーの追加）を必要としない。IIS実機での追加確認は
  今回のセッションでは行っていない（`.wasm`/`.mjs`のMIME設定については既存README記載のとおり）
- `script-src`/`style-src`の`'unsafe-inline'`は、スライド描画用ドキュメント（`srcdoc`の
  iframe・`window.open('')`で開くプレゼン専用ウィンドウ）とMarpが生成するインライン
  `<script>`/`<style>`に必要なため許可している。これらはIIS（HTTP配信）・GitHub Pages
  （HTTPS配信）のどちらでも同じ`srcdoc`/`window.open('')`の仕組みを使うため、配信方式による
  差異は無い
- `img-src`は`http:`/`https:`を含めており、Markdown内の外部URL画像は既存README
  「外部通信について（外部URL画像の注意）」の記載どおり、CSP追加後も変わらず表示できる
  （閉域環境では引き続き利用者の注意が必要）
- `connect-src 'self'`により、万一Markdown内や依存ライブラリの経路で`fetch`/`XMLHttpRequest`が
  発生しても外部ホストへは届かない。これはIIS（閉域環境）・GitHub Pages（インターネット
  接続あり）のどちらの配信であっても、MarkdownUtil自身が外部通信を行わないという
  既存方針（README「閉域環境での利用について」）を、ブラウザ側でも強制する形になる
- GitHub Pages（HTTPS）・IIS（HTTP、庁内閉域環境）のいずれでも、`window.origin`は
  `http:`/`https:`いずれのスキームでも`'null'`にならない値を返すため、
  `createMessagePort()`のorigin指定ロジック（`window.origin && window.origin !== 'null' ? window.origin : '*'`）は
  同じ経路で実オリジンを使う。`file://`で直接開いた場合のみ`origin`が`'null'`になり、
  从来通り`'*'`にフォールバックする（HTTPS前提のAPIへは変更していない）

### 外部通信確認

ブラウザ回帰・セキュリティ確認のいずれも、Playwrightで全リクエストを記録し、
配信元オリジン（`http://127.0.0.1:8801`）とBlob URL以外への通信が0件であることを
確認した（文書変換・プレビュー描画・コピー・保存・スライド表示・HTML出力・印刷・
プレゼン表示・ヘルプの一連操作を含む）。

### 既知の制限（v0.6.4で追加）

- 別オリジンのページが実際に`window.open()`でMarkdownUtilの窓を取得し、偽装した
  `render`メッセージを送りつける攻撃そのものは、`createMessagePort()`の
  ユニットテストで送信元window・オリジンの検証ロジックを直接確認したにとどまり、
  2オリジンを用意した実ブラウザでのエンドツーエンド攻撃再現は行っていない。
- IIS実機での`Content-Security-Policy`（`<meta>`タグ）の動作確認は、今回のセッションでは
  実施していない（IIS特有のヘッダー付与や`web.config`との相互作用は無いはずだが、
  実機配置後の確認を推奨する）。
- テーマ変更・キーボード操作・focus trap・背景クリック・多重クリック防止等、
  今回コードを変更していない機能については、v0.1.0〜v0.6.3で確認済みの内容を
  再確認していない（実装箇所そのものに変更が無いため）。
